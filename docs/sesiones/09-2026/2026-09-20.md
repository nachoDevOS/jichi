# Sesión de trabajo — 2026-09-20

> Registro de trabajo realizado por **nachoDev**. Rama: `nachoDev`.

---

## Trabajo 1 — El recibo es UNO por trámite de aprovechamiento, no uno por depósito

**Commit:** `[pendiente]` — `fix(aprovechamientos): un recibo por tramite, emitido al enviar a revision`

### Problema

En la ficha del aprovechamiento (`/panel/aprovechamientos/1`), dos depósitos
cargados contra el mismo cupo aparecían como **dos recibos numerados**:
`REC-2026-0001` (330,00 Bs) y `REC-2026-0002` (82,50 Bs).

El aprovechamiento es un **trámite**: se otorga, se paga y se presenta. Su
comprobante es **uno solo**, con el total de todos los depósitos, sin importar si
la persona pagó con una boleta o con cinco. Emitiendo uno por depósito:

- la persona se iba con tres papeles de un solo trámite,
- se gastaban tres números de una serie que Contabilidad audita,
- el arqueo del día mostraba tres cobros donde hubo uno.

**Y había un segundo problema en el mismo lugar:** el control de «no cobrar de
más» corría por llamada al servicio, así que tres depósitos por el monto total
cada uno pasaban los tres —ninguno veía a los anteriores, que todavía no estaban
escritos—.

### El circuito que quedó

```
PENDIENTE   ──< depósito 330,00 (boleta 1242134)   recibo_id NULL
            ──< depósito  82,50 (boleta 42341234)  recibo_id NULL
     │
[enviar a revisión] ──▶ REC-2026-0001 · 412,50 Bs ──< los dos depósitos
     │
EN REVISIÓN ──[aprobar]──▶ ACTIVO
```

El recibo sale **al presentar el expediente**, que es el momento real: el
pescador entrega sus boletas y se va con el comprobante. Antes de eso los
depósitos ya están cargados y todavía no hay papel que ponerles.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `database/migrations/2026_09_18_100900_create_pagos_table.php` | `recibo_id` pasa a **nullable**: el depósito nace antes que el recibo |
| `app/Services/CobrarService.php` | Dos métodos nuevos: `registrarDepositos()` —carga los depósitos con `recibo_id` en NULL y descuenta el saldo entre ellos— y `emitirRecibo()` —crea el papel y le cuelga todos los pagos sueltos del trámite— |
| `app/Services/RevisarCupoService.php` | `enviar()` emite el recibo **dentro de la misma transacción** que el cambio de estado, y acepta el NIT/nombre de factura |
| `app/Http/Controllers/Panel/AprovechamientoController.php` | `pagar()` ya no llama a `cobrar()` en un `foreach`; `show()` manda el recibo del trámite como prop propia |
| `app/Exceptions/CobroInvalidoException.php` | `noAdmiteDepositos()`: mensaje propio para el trámite presentado o aprobado |
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | El número de recibo deja de ser una columna repetida y pasa a una cabecera única sobre la tabla de depósitos |
| `resources/js/pages/panel/caja/index.tsx` | Un pago sin recibo se muestra como «Sin recibo», sin enlace |
| `resources/js/types/aprovechamientos.ts`, `resources/js/types/caja.ts` | `ReciboDelCupo`; `recibo_id` admite NULL |
| `docs/MER.md` | Por qué `pagos.recibo_id` es nullable |

### Solución

**Antes** — una llamada al cobro por depósito, y cada llamada emitía su recibo:

```php
foreach ($datos['pagos'] as $i => $pago) {
    $caja->cobrar(
        [['tipo' => 'cupo', 'id' => $aprovechamiento->id, 'monto' => (float) $pago['monto']]],
        $nit, $nombre, $pago['nro_transaccion'], $pago['fecha_deposito'], $subidos[$i],
    );
}
```

**Después** — los depósitos se cargan sin papel, y el papel lo emite el envío:

```php
// AprovechamientoController::pagar()
$caja->registrarDepositos($aprovechamiento, $depositos);   // recibo_id = NULL

// RevisarCupoService::enviar(), dentro de la transacción del cambio de estado
$this->caja->emitirRecibo($bloqueado, $nitCi, $nombreFactura);
```

#### El recibo se emite dentro de la transacción del envío

No al lado. Un recibo emitido sobre un cupo que se quedó en pendiente sería un
papel entregado por un expediente que nadie presentó; y al revés —presentado sin
papel— la persona se va con las manos vacías. O pasan las dos cosas o ninguna.

#### Un reenvío NO emite un segundo recibo

`emitirRecibo()` solo toma los pagos con `recibo_id` en NULL y devuelve `null` si
no hay ninguno. Un cupo rechazado vuelve a PENDIENTE con sus depósitos y su papel
ya emitido, así que al presentarlo de nuevo no se toca el correlativo y el número
que la persona tiene en la mano sigue siendo el suyo. Es el mismo criterio que
tenía `fecha_revision` en el modelo anterior.

#### El saldo se descuenta entre depósitos del mismo envío

Los depósitos que se mandan juntos se validan contra un saldo que todavía no los
incluye. `registrarDepositos()` arranca del `saldoPendiente()` y lo va bajando
con cada uno: si entre todos se pasan, el cobro entero se rechaza y la
transacción no deja nada a medias.

#### Se le pregunta al MODELO si admite pagos, no al enum

`resolver()` usa `method_exists($estado, 'admitePagos')`, y `EstadoAprovechamiento`
declara ese método con otro nombre —`permitePagos()`—, así que la comprobación
devolvía «no existe» y dejaba pasar cualquier estado. El modelo sí expone
`admitePagos()`. Con la comprobación vieja, un depósito cargado sobre un cupo EN
REVISIÓN además contestaba «el papel está anulado», que manda a buscar una
anulación que nunca existió: por eso el mensaje nuevo.

#### En la pantalla, el número de recibo dejó de ser una columna

Estaba repetido en cada fila de la tabla de depósitos, y eso se leía como «un
recibo por depósito» —que además era lo que pasaba—. Ahora va una sola vez,
arriba del detalle que ampara, con el total y el botón de imprimir. Mientras el
cupo está pendiente, ese recuadro dice cuándo va a salir en vez de dejar un
hueco.

### Verificación

**El esquema no se rearmó.** `pagos.recibo_id` se puso nullable en `jichi1` con
`ALTER TABLE pagos ALTER COLUMN recibo_id DROP NOT NULL`, que no toca los datos
ni el orden de las columnas; la migración quedó igual para una instalación nueva.

El circuito completo se probó sobre un esquema armado desde cero en una base
descartable (SQLite en el directorio temporal, **nunca** la base de trabajo):

| Caso | Resultado |
|---|---|
| Dos depósitos cargados | 0 recibos, 2 pagos con `recibo_id` NULL |
| Enviar con saldo pendiente | Frenado, y **sin** emitir recibo |
| Tercer depósito y envío | **1 recibo**, 3 pagos adentro, total 55,00 = la suma |
| Rechazar y volver a enviar | Sigue habiendo **1** recibo, con su número original |
| Aprobar | Pasa a ACTIVO |
| Dos depósitos que juntos exceden el saldo | Rechazado, y el rollback no deja ningún pago |
| Cargar un depósito con el cupo EN REVISIÓN | Rechazado, con el motivo correcto |
| Cobro desde Caja | Sigue emitiendo su recibo en el acto, sin cambios |

`npx tsc --noEmit`, `./vendor/bin/pint` y `npm run build`, los tres en verde.

**Falta probarlo en el navegador.** Los dos recibos que ya están en `jichi1`
—`REC-2026-0001` y `REC-2026-0002` del cupo 1— son cobros válidos repartidos en
dos papeles: quedan como están, salvo que se decida unificarlos a mano.

---

## Trabajo 2 — El control de las boletas en EN REVISIÓN: validar, observar, corregir

**Commit:** `[pendiente]` — `feat(pagos): control de boletas con validado_por y validado_en`

### Problema

Con el recibo emitido al enviar a revisión, el expediente quedaba presentado y
listo para firmar **sin que nadie hubiera mirado las boletas**. Aprobar era
creerle al número que alguien tipeó en el mostrador: el monto cubierto dice
cuánto se DECLARÓ, no que esa plata haya entrado al banco.

Faltaba la otra mitad de la revisión —comparar cada depósito contra el extracto y
dejar registrado **quién** lo dio por bueno y **cuándo**—, y un lugar donde
resolver lo que no cuadra sin tener que devolver el expediente entero.

### El circuito que quedó

```
PENDIENTE ──▶ VALIDADO    la boleta cuadra con el extracto del banco
    ▲     └─▶ OBSERVADO   no cuadra, con el motivo escrito
    └──[corregir]──┘
```

Y la firma ahora exige **tres** cosas, no dos: estado EN REVISIÓN, monto cubierto
y **todas las boletas validadas**. Sin la tercera, validar sería decorativo.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Enums/EstadoValidacionPago.php` | **Nuevo.** `pendiente` \| `validado` \| `observado`, con su etiqueta y color |
| `database/migrations/2026_09_18_100900_create_pagos_table.php` | Cinco columnas: `registrado_por`, `validado_por`, `estado_validacion`, `observacion`, `validado_en` |
| `app/Models/Pago.php` | Casts, el default en `$attributes`, las dos relaciones a `users`, `admiteControl()`, `admiteCorreccion()` y el scope `sinValidar()` |
| `app/Traits/Pagable.php` | `admiteControlDePagos()` y `admiteCorreccionDePagos()`, en `false` por defecto |
| `app/Models/AprovechamientoPesq.php` | Los sobreescribe: controlar solo EN REVISIÓN, corregir con el expediente abierto |
| `app/Enums/EstadoAprovechamiento.php` | `estaAbierto()` —pendiente + en revisión—, con el aviso de que NO es permiso de escritura |
| `app/Services/ControlarPagoService.php` | **Nuevo.** Las tres operaciones, con la fila bloqueada |
| `app/Services/RevisarCupoService.php` | `aprobar()` exige que no quede ninguna boleta sin validar |
| `app/Services/CobrarService.php` | Los depósitos nacen con `registrado_por` |
| `app/Http/Controllers/Panel/PagoController.php` | **Nuevo.** `validar`, `observar` y `corregir` |
| `app/Http/Requests/Panel/{Observar,Corregir}PagoRequest.php` | **Nuevos.** Motivo obligatorio; boleta única ignorándose a sí misma |
| `app/Exceptions/{Cobro,Cupo}InvalidoException.php` | Mensajes que dicen QUÉ hacer, no solo que no se puede |
| `app/Enums/RolSistema.php` | `pagos.controlar` (supervisión) y `pagos.corregir` (ventanilla) |
| `routes/panel.php` | Las tres rutas, cada una con su permiso |
| `resources/js/components/panel/pagos/dialogo-corregir-pago.tsx` | **Nuevo.** La ventana de corrección, con la observación a la vista |
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | Columna «Validación», los tres botones, la ventana de observación y «Aprobar» apagado mientras falte validar |
| `resources/js/types/aprovechamientos.ts` | Los campos del control |
| `docs/MER.md` | Las columnas nuevas y por qué el control no es el estado del pago |

### Solución

#### Un observado SIGUE SUMANDO en el saldo

Es lo primero que hay que entender del estado. Que el dinero entró ya lo dice que
la fila exista; el control contesta otra pregunta: si alguien MIRÓ esa boleta.
Sacar un observado de la suma dejaría al trámite figurando sin cubrir por una
observación que puede estar equivocada.

#### Un observado no se valida: se corrige

`EstadoValidacionPago::admiteControl()` solo deja pasar lo que nadie miró, así que
el botón «Validar» **no aparece** sobre un observado. Darlo por bueno sin que el
dato cambie es aprobar justo lo que se marcó como malo, y el reparo se pierde sin
que quede si se arregló.

Al corregirlo vuelve a PENDIENTE y se le borra el control **entero** —también
`validado_por` y `validado_en`—: quien validó lo hizo sobre otros números, y
dejar su nombre al lado del dato nuevo le atribuye algo que no miró.

Si la observación estaba equivocada, se abre «Corregir» y se guarda sin cambiar
nada: queda en `auditorias` quién lo hizo.

#### Corregir vive en la FICHA, no en «editar»

Observar solo pasa EN REVISIÓN, y esa pantalla no abre ahí. Con el botón en un
solo lado, el reparo no tendría salida y el expediente quedaría trabado: habría
que rechazarlo entero para arreglar un número de boleta.

#### Dos permisos, y ese es el punto

`pagos.controlar` es de **supervisión** —quien firma mira las boletas— y
`pagos.corregir` es de **ventanilla** —quien las cargó arregla lo que tipeó—. Con
uno solo, la misma persona objetaría y resolvería su propia objeción.

#### El botón «Aprobar» se apaga mientras falte validar

Con el `title` diciendo cuántas faltan. El servidor exige la condición igual; el
botón se apaga para que no prometa algo que va a fallar, que es peor que no
ofrecerlo — el revisor aprieta, lee un error rojo y no sabe qué le falta mirar.

#### Las dos preguntas del control van en el trait, con `false` por defecto

Y no con un `method_exists` en quien pregunta. Ese truco deja el control APAGADO
en silencio el día que alguien escribe el método con otro nombre — que es
exactamente lo que ya había pasado con `admitePagos()` contra
`EstadoAprovechamiento::permitePagos()`, corregido en este mismo trabajo.

### Verificación

**El esquema no se rearmó.** Las cinco columnas se agregaron a `jichi1` con
`Schema::table()`, sin tocar los datos (los 2 depósitos siguen ahí). Quedan al
final de la tabla en vez de en el orden de la migración; en una instalación nueva
salen ordenadas como dice el archivo.

Circuito completo sobre un esquema armado desde cero en base descartable:

| Caso | Resultado |
|---|---|
| Depósito recién cargado | `pendiente`, con `registrado_por` escrito |
| Validar con el cupo PENDIENTE | Frenado: el control es parte de la revisión |
| Aprobar con 2 sin validar | Frenado, diciendo cuántas faltan |
| Validar | `validado`, con quién y con hora |
| Volver a validar el mismo | Frenado |
| Observar con motivo | `observado`, motivo guardado, y el **saldo sigue en 0** |
| Validar un observado | Frenado, explicando que primero se corrige |
| Aprobar con un observado | Frenado |
| Corregir | Vuelve a `pendiente`, sin observación y sin validador |
| Validar y aprobar | ACTIVO |
| Tras aprobar | Ni control ni corrección |

Y contra la base real (`jichi1`, solo lectura): el cupo 1 muestra sus 2 depósitos
«Sin validar», los dos validables y corregibles, y «Aprobar» apagado.

`npx tsc --noEmit`, `./vendor/bin/pint` y `npm run build`, los tres en verde.

**Falta probarlo en el navegador**, que es lo único que prueba los botones.

---

## Trabajo 3 — Se sacó «Efectivo» del recibo oficial

**Commit:** `[pendiente]` — `fix(recibos): quitar la casilla de efectivo del recibo oficial`

### Problema

El recibo calcaba el renglón del talonario de papel:

```
Depósito Bancario: [X]   Efectivo [ ]   N°: 1242134
```

La casilla de Efectivo salía **vacía en todos los recibos**, porque en esta
unidad no se cobra en efectivo ni por QR: todo pago es un depósito bancario. Era
una opción impresa que nunca se podía marcar, y lo único que produce es la
pregunta de por qué no se usa.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `resources/views/documentos/recibo-oficial.blade.php` | Se quitaron el rótulo «Efectivo» y su casilla; la de depósito queda marcada fija |
| `app/Http/Controllers/Panel/ReciboController.php` | Ya no se manda la variable `esDeposito`, que valía siempre `true` |

### Solución

El renglón quedó así, y el espacio liberado se lo lleva el número de boleta —que
es el dato que se cruza contra el extracto del banco y el que más se quedaba
corto—:

```
Depósito Bancario: [X]   N°: 1242134
```

Mismo criterio que la columna `metodo_pago` que `pagos` no tiene: un dato con un
solo valor posible no informa nada, e invita a suponer que alguna vez hubo otra
cosa.

### Verificación

Se renderizó la plantilla con un recibo real: no queda ninguna aparición de
«Efectivo», y el renglón sale con la casilla de depósito marcada y la boleta
completa. El PDF se generó entero (82 KB) después de sacar la variable, así que
no quedó ninguna referencia suelta.

---

## Trabajo 4 — Los comentarios: de 12.695 líneas a 5.565

**Commit:** `[pendiente]` — `docs: podar comentarios y mover el detalle a NOTAS-CODIGO`

### Problema

**El 38% del código eran comentarios**: 12.695 líneas sobre 33.383, en 198
archivos. Había bloques de treinta y cuarenta líneas —con banners de `====`,
títulos en mayúscula y párrafos de historia— para explicar una sola línea de
código. Los archivos dejaron de leerse de corrido: `CarnetImpresionController`
tenía 579 líneas de comentario sobre 862, y `carnet-pescador.blade.php`, 567
sobre 763.

Lo pidió el responsable del proyecto, y tiene razón: un comentario que hay que
desplazar media pantalla para pasarlo ya no ayuda a leer el código.

### Solución — en tres pasadas, sin perder una palabra

| Pasada | Qué hizo | Líneas |
|---|---|---:|
| 1. Decoración | Saca banners de `====` y `----`, líneas vacías repetidas y las vacías al abrir y cerrar cada bloque. **No toca una sola palabra.** | −804 |
| 2. Poda (bloques de más de 8 líneas) | Deja el **primer párrafo** en el código y manda el resto a `docs/NOTAS-CODIGO.md` | −5.137 |
| 3. Poda (bloques de más de 6) | Lo mismo, más bajo, y reconociendo el estilo `\| … \|` de `routes/` | −1.190 |

**Nada se borró: se movió.** Lo podado vive en
[docs/NOTAS-CODIGO.md](../../NOTAS-CODIGO.md) —7.118 líneas— agrupado por archivo
y con el título del bloque, así que se encuentra con un `grep`. Los `@param` y
`@return` se quedaron en el código, que es donde los lee el analizador.

**Resultado: 5.565 líneas de comentario, el 21%.** Y en `CLAUDE.md` la regla 2
pasó a decir de una a tres líneas, con el porqué, para que no vuelvan a crecer.

### Verificación

`php -l` sobre los 198 archivos, `./vendor/bin/pint --test`, `npx tsc --noEmit`,
`npm run build` y `php artisan view:cache` —que compila las 60 plantillas Blade—,
todos en verde. Y se volvió a generar el PDF del recibo contra la base real: 82
KB, con sus dos boletas.

---

## Trabajo 5 — La AUTORIZACIÓN DE PESCA, en PDF

**Commit:** `[pendiente]` — `feat(aprovechamientos): autorizacion de pesca en PDF`

### Problema

Al aprobar un aprovechamiento, el pescador se tiene que llevar la
**Autorización de Pesca para Aprovechamiento Pesquero** — el formulario del
talonario del SEDAG— y el sistema no lo emitía: se seguía llenando a mano.

### Archivos nuevos

| Archivo | Qué hace |
|---|---|
| `resources/views/documentos/autorizacion-pesca.blade.php` | El calco del papel: encabezado, campos punteados, tabla de tamaños mínimos, reglas de redes y el pie con la fecha |
| `app/Http/Controllers/Panel/AutorizacionPescaController.php` | Arma el PDF y lo manda al navegador |
| `public/image/autorizacion-logo.png` | Copia a medida del logo del SEDAG |

Y se tocaron `routes/panel.php` (la ruta), `RolSistema` (el permiso),
`AprovechamientoPesq::yaFueAprobado()`, `AprovechamientoController::resumir()`
(la bandera) y `ver.tsx` (el botón).

### Solución

**Sale recién con el cupo APROBADO.** La regla vive en
`AprovechamientoPesq::yaFueAprobado()` y la usan las dos puntas: el botón de la
ficha y el controlador, que la vuelve a comprobar —esconder el botón en React es
comodidad, la dirección se escribe a mano—.

Cuenta como aprobado **activo, vencido y agotado**: a los dos últimos se llega
desde ACTIVO, así que los tres pasaron por la firma. Un cupo vencido se
reimprime igual, por si hay que reponer el papel de una gestión cerrada.

**Permiso propio, `aprovechamientos.imprimir`**, como `carnets.imprimir`:
entregar el papel es un acto distinto de consultar la ficha.

**Qué llena cada renglón:** el nombre, el domicilio, la cédula con complemento y
expedido, el tipo de embarcación, el volumen en kilos, la vigencia
—`fecha_emision` a `fecha_vencimiento`—, el valor de la concesión y la fecha del
último depósito como tiempo de cancelación.

**El número es el id del cupo con ceros a la izquierda** y no un correlativo
propio: el talonario de papel ya trae el suyo impreso, y un contador aparte se
gastaría en cada reimpresión o pediría una columna más. Así una reimpresión sale
siempre con el mismo número.

**La tabla de tamaños mínimos y las reglas de redes van como constantes del
controlador**, no en la base: son texto del reglamento, preimpreso en el
talonario. Si una resolución los cambia, se tocan ahí.

**La fecha del pie sale de `fecha_emision` y no de hoy**, o una reimpresión del
mes que viene diría otra cosa que el papel entregado.

### Verificación

Se renderizó contra la base real (cupo 1, Rosa Alfaro Oquendo) y se comparó
contra la foto del talonario: **una sola página, carta vertical (612×792 pt)**,
con el sello de agua, la tabla de 13 especies y las cinco reglas de redes.

| Caso | Resultado |
|---|---|
| Cupo aprobado | PDF de 129 KB, una página |
| Cupo pendiente | `RedirectResponse` con el aviso, sin PDF |
| Permiso sembrado | `aprovechamientos.imprimir` ✔ |

El logo del SEDAG venía de `sedag.png` —2048 px, 1 MB— y hacía un **PDF de 3
MB**; con la copia reducida quedó en 129 KB. Es la misma trampa que ya habían
dado los PNG del panel en el recibo.

`php -l`, `./vendor/bin/pint`, `npx tsc --noEmit` y `npm run build`, en verde.

---

## Trabajo 6 — Un solo estilo de botón para Ver, Editar y Eliminar

**Commit:** `[pendiente]` — `feat(ui): variantes ver/editar/eliminar en Button`

### Problema
Cada pantalla se inventaba el aspecto de sus acciones: en la ficha del cupo
«Editar» era ámbar y «Eliminar» rojo con las clases escritas a mano; en otras
pantallas los mismos botones salían grises, y en carnets y guías la acción
destructiva era un botón rojo lleno. Las mismas tres acciones se veían de tres
formas distintas según la pantalla.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/js/components/ui/button.tsx` | Variantes `ver`, `editar` y `eliminar` |
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | Usa las variantes; «Ver al pescador» lleva ícono |
| `resources/js/pages/panel/aprovechamientos/index.tsx` | Las tres acciones de la tabla |
| `resources/js/pages/panel/beneficiarios/index.tsx` | Ver y editar de la tabla |
| `resources/js/pages/panel/beneficiarios/ver.tsx` | Editar y dar de baja |
| `resources/js/pages/panel/carnets/ver.tsx` | Ver al titular y revocar |
| `resources/js/pages/panel/faenas/ver.tsx` | Ver al pescador y ver cupo |
| `resources/js/pages/panel/guias/ver.tsx` | Ver al comercializador y anular |
| `resources/js/pages/panel/catalogos/*.tsx` | El lápiz de cada tabla |

### Solución
El color pasó a ser parte del componente: celeste mira, ámbar cambia, rojo saca.
`eliminar` cubre toda acción que retira algo de circulación —eliminar, dar de
baja, anular, revocar, rechazar—, así que las clases ya no se copian por
pantalla. Las tres llevan borde, también en la columna de acciones de una tabla.

**Antes:**
```tsx
<Button variant="outline" className="border-amber-300 text-amber-700 hover:bg-amber-50 ...">
```

**Después:**
```tsx
<Button variant="editar">
```

---

## Trabajo 7 — La ficha del cupo decía «vencido o sin saldo» sobre un cupo recién creado

**Commit:** `[pendiente]` — `fix(aprovechamientos): el motivo real por el que no emite faenas`

### Problema
Un cupo recién otorgado —PENDIENTE, sin cobrar— mostraba en «Situación»: «No se
le pueden emitir faenas: el cupo está vencido o sin saldo». Las dos cosas eran
falsas: vence el 31/12 y tiene los 200 kg intactos. Lo único que falta es
cobrarlo y firmarlo. El texto era el único que había para todos los noes de
`puedeEmitirFaena()`, así que mandaba a buscar un problema que no existía.

Y debajo salía la tarjeta «Faenas emitidas» con «Sin faenas», sobre un cupo que
todavía no puede tener ninguna.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/AprovechamientoPesq.php` | `motivoSinFaena()`: el porqué, o null si sí puede |
| `app/Http/Controllers/Panel/AprovechamientoController.php` | Manda `motivo_sin_faena` |
| `resources/js/types/aprovechamientos.ts` | El campo nuevo |
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | Usa el motivo; la tarjeta de faenas y las cifras de saldo, solo desde la firma |
| `resources/js/components/panel/aprovechamientos/barra-saldo.tsx` | Sin firma dice «kg solicitados» y no dibuja barra |
| `app/Http/Controllers/Panel/BeneficiarioController.php` · `types/beneficiarios.ts` | El resumen del cupo manda `ya_fue_aprobado` |
| `resources/js/pages/panel/beneficiarios/ver.tsx` | Mismo criterio en su lista de cupos |

### Solución
El motivo lo resuelve el servidor y llega listo, como el resto de los `puede_*`:
pendiente y en revisión dicen qué falta, vencido dice que venció y agotado que
se quedó sin kilos. La pantalla no vuelve a evaluar el estado.

La tarjeta «Faenas emitidas» aparece desde que el cupo pasó por la firma
(`ya_fue_aprobado`), o antes si por algún motivo ya tuviera filas.

**Y el saldo tampoco va antes de la firma.** «Consumido 0 kg · Disponible
200 kg · Usado 0%», con la barra llena, describe un movimiento que no puede
ocurrir: el cupo no autoriza a pescar todavía. Lo que hay es un volumen PEDIDO,
así que mientras no esté aprobado se muestra `200 kg solicitados` y nada más. La
regla vive en `BarraSaldo` —una sola vez para el listado y la ficha— y el mismo
criterio se aplicó al resumen de cupos de la ficha del beneficiario, que dibuja
su barra a mano.

**Ni después de la firma, mientras no se haya pescado.** Un cupo aprobado sin
una sola faena mostraba «300 / 300 kg» con la barra vacía de punta a punta
—que se lee como un cupo GASTADO, justo lo contrario— y un desglose donde los
cuatro números decían lo mismo. Con `kilos_consumidos` en cero sale
`300 kg otorgados · sin faenas emitidas`, y el desglose aparece recién cuando
hay algo que desglosar.

**Antes:**
```tsx
: 'No se le pueden emitir faenas: el cupo está vencido o sin saldo.'
```

**Después:**
```tsx
: cupo.motivo_sin_faena
```

---

## Trabajo 8 — Cargar un depósito: el monto arranca vacío y el «sin pagos» se calla

**Commit:** `[pendiente]` — `fix(aprovechamientos): el formulario de depósitos`

### Problema
Dos cosas en la tarjeta de Pagos de la ficha del cupo:

1. «Agregar pago» abría la sección con el monto **ya escrito** —el saldo entero,
   110,00— y un campo lleno se confirma sin leerlo. El monto es el que dice la
   BOLETA: si la persona depositó 100, el formulario ya proponía otra cifra.
2. Debajo del formulario abierto seguía el cartel «Sin pagos registrados», que
   se lee como que lo que se está tipeando no entró.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | `seccionNueva()` sin monto propuesto; el vacío no se muestra con el formulario abierto |

### Solución
`seccionNueva()` ya no recibe el saldo: el monto nace en blanco, con
`placeholder="0.00"`. Lo que falta cubrir se sigue viendo abajo, en «Suman X de
Y pendientes», que es donde corresponde. El estado vacío se condiciona además a
`!cobrando`.

**Antes:**
```tsx
monto: monto > 0 ? String(monto) : '',
```

**Después:**
```tsx
monto: '',
```

---

## Trabajo 9 — El N° de transacción acepta solo dígitos

**Commit:** `[pendiente]` — `feat(pagos): el número de boleta es numérico`

### Problema
El número de la boleta del banco es un número, y el campo aceptaba cualquier
texto. Tipeado con una letra de más, la boleta queda cargada con un número que
no existe y el control de unicidad —«esta boleta ya está cargada»— deja de
servir: `00123A` y `00123` conviven como dos boletas distintas.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Requests/Panel/RegistrarPagoCupoRequest.php` | `digits_between:1,60` + su mensaje |
| `app/Http/Requests/Panel/CobrarRequest.php` | Ídem |
| `app/Http/Requests/Panel/CorregirPagoRequest.php` | Ídem |
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | `soloDigitos()` + `inputMode="numeric"` |
| `resources/js/pages/panel/caja/cobrar.tsx` | Ídem |
| `resources/js/components/panel/pagos/dialogo-corregir-pago.tsx` | Ídem, y el campo va monoespaciado como los otros dos |

### Solución
El campo sigue siendo de TEXTO y no `type="number"`: la boleta suele empezar
con ceros y un campo numérico se los come. Lo que no sea dígito no entra al
escribir, e igual se valida en el servidor —la pantalla es comodidad, no
seguridad—. En las reglas va `digits_between` y no `numeric`, por lo mismo de
los ceros.

**Antes:**
```php
'required', 'string', 'max:60',
```

**Después:**
```php
'required', 'string', 'digits_between:1,60',
```

---

## Trabajo 10 — Fecha de SOLICITUD y fecha de OTORGAMIENTO son dos cosas distintas

**Commit:** `[pendiente]` — `feat(aprovechamientos): fecha_solicitud + fecha_emision al aprobar`

### Problema
El cupo tenía una sola fecha, `fecha_emision`, escrita al crearlo, y la ficha la
mostraba como «Otorgado el 20/09/2026» sobre un expediente PENDIENTE que nadie
había firmado —y que podía terminar rechazado—. El día que se pide y el día que
se otorga no son el mismo dato.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `database/migrations/…_create_aprovechamientos_pesq_table.php` | `fecha_solicitud` nueva; `fecha_emision` pasa a nullable |
| `app/Models/AprovechamientoPesq.php` | `#[Fillable]` y cast |
| `app/Services/OtorgarCupoService.php` | Escribe la solicitud; la emisión nace en NULL |
| `app/Services/RevisarCupoService.php` | `aprobar()` escribe la emisión y recalcula el vencimiento |
| `app/Http/Requests/Panel/OtorgarCupoRequest.php` | El campo del formulario es `fecha_solicitud` |
| `app/Http/Controllers/Panel/AprovechamientoController.php` · `BeneficiarioController.php` | Payloads y orden del listado |
| `resources/js/pages/panel/aprovechamientos/{crear,editar,ver}.tsx` · `types/*` | El rótulo y los dos renglones de la ficha |
| `docs/MER.md` | La tabla y el porqué |

### Solución
`fecha_emision` queda en NULL hasta la aprobación: en NULL significa «todavía no
se otorgó», sin posibilidad de contradicción —mismo criterio que las fechas de
impreso y entregado del carnet—. La ficha muestra «Solicitado el» siempre y
«Otorgado el» solo cuando existe.

Los listados que ordenaban por `fecha_emision` pasan a `fecha_solicitud`: con la
emisión en NULL, ordenar por ella deja cualquier cosa arriba.

**El vencimiento se recalcula al aprobar**, sobre la emisión: el cupo vale por
la gestión, así que un expediente pedido el 28/12 y firmado en enero vence con
el año nuevo.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`. El esquema
> real queda viejo hasta entonces y la pantalla revienta con
> `column "fecha_solicitud" does not exist`. Verificado antes en una base
> descartable de SQLite, y probado el circuito entero —otorgar, cobrar, enviar,
> validar, aprobar— sobre ella.

---

## Trabajo 11 — La barra de filtros del listado de cupos, igual a la del padrón

**Commit:** `[pendiente]` — `style(aprovechamientos): la barra de filtros del padrón`

### Problema
El listado de cupos tenía sus filtros en una fila de elementos sueltos —campo,
dos listas y un botón con lupa— mientras el padrón de beneficiarios usa una
barra ordenada sobre doce columnas: «Mostrar N registros» a la izquierda y el
buscador con lupa adentro a la derecha. Dos pantallas con la misma función y
dos maquetas distintas.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/js/pages/panel/aprovechamientos/index.tsx` | La barra pasa a la maqueta del padrón |

### Solución
La misma grilla de doce columnas: «Mostrar» a la izquierda (4) y, a la derecha
(8), el filtro de estado y el buscador **en un mismo grupo flex**: los dos
filtran lo mismo —qué filas se ven— y separados por media pantalla se leían como
dos controles sin relación. La lupa va dentro del campo y el botón aparte se
retiró: el buscador es un `<form>` y el Enter lo envía, igual que en el padrón.

La tabla suma **Solicitado** y **Otorgado** junto a **Vence**; la segunda sale
con un guion mientras nadie haya firmado el trámite.

---

## Trabajo 12 — El aprovechamiento se cobra ENTERO: no se registra un depósito parcial

**Commit:** `[pendiente]` — `feat(pagos): los depósitos del cupo tienen que cubrir el monto`

### Problema
Con 150 Bs cargados de 165, el botón decía «Registrar 1 depósito(s)» y lo
guardaba igual: el expediente quedaba a medio cobrar, sin recibo —el recibo sale
al enviar a revisión—, sin poder enviarse y sin ninguna señal fuera de la ficha.
El saldo se descubría recién al volver a abrirla.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/Panel/AprovechamientoController.php` | `pagar()` lo frena antes de subir archivos |
| `app/Services/CobrarService.php` | `registrarDepositos()` exige que la SUMA cubra el saldo, con la fila bloqueada |
| `app/Exceptions/CobroInvalidoException.php` | `noCubreElMonto()` |
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | El botón se apaga y dice cuánto falta |
| `docs/MER.md` | La regla, junto a la tabla |

### Solución
Los depósitos se miran como CONJUNTO y no uno por uno: se pueden cargar varias
boletas —la persona depositó en dos veces— pero entran todas juntas y tienen que
sumar el saldo entero.

**El control está en TRES capas, y el botón apagado no es ninguna de ellas** —se
quita desde el inspector del navegador—: el controlador lo comprueba **antes de
subir los archivos** (si fallara adentro habría que borrar lo ya escrito), y el
servicio lo vuelve a comprobar con la fila BLOQUEADA, que es el único control que
resiste dos ventanillas cobrando a la vez. La ruta ya pedía `permiso:caja.cobrar`.

De paso se separó `cubierto` de `cubre`: cubrir el monto habilita REGISTRAR, y
enviar pide además el permiso `aprovechamientos.enviar`. Estaban en una sola
variable, así que a quien no puede enviar se le habría apagado el botón de
cargar boletas.

**De MÁS sí se admite**, y por pedido expreso: la boleta del banco dice lo que
dice, y el excedente queda a favor de la entidad. El tope por línea
(`excedeElSaldo()`) se sacó de este camino —trababa un expediente con la plata ya
depositada— y en su lugar quedó `yaEstaPagado()`: sobre un cupo ya cubierto no
se cargan más boletas. La cuenta de la pantalla dice cuánto sobra.

**En Caja no cambió nada**: ahí se cobra línea por línea, un parcial se sigue
admitiendo y el tope de no cobrar de más sigue puesto, porque ese control lo
comparten carnets y guías.

---

## Trabajo 13 — El recibo impreso lleva la cédula con su EXPEDIDO

**Commit:** `[pendiente]` — `fix(recibos): la cédula del recibo va completa`

### Problema
En el renglón «C.I.» del recibo salía `1234567`, sin complemento ni expedido,
porque el recibo copiaba `beneficiarios.ci` a secas. El talonario verde se llena
con la cédula entera —«1234567-1A BN»—, que es la que identifica a la persona en
un control.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Services/RevisarCupoService.php` | El recibo copia `documento_identidad`, no `ci` |
| `resources/js/pages/panel/caja/cobrar.tsx` · `CajaController.php` | El campo del comprobante se propone igual; se quitó la prop `ci`, que ya no usaba nadie |

### Solución
Se usa el accesor `Beneficiario::documentoIdentidad()`, que ya armaba
«ci-complemento expedido» para el carnet. El dato sigue siendo una COPIA
congelada en `recibos.nit_ci_factura` y sigue siendo editable: el comprobante
puede salir a nombre de un tercero, con su NIT.

Los recibos ya emitidos no cambian —por eso son copias— así que el renglón
completo se ve en los que se emitan desde ahora.

---

## Trabajo 14 — El recibo guarda el ID del beneficiario, no su nombre ni su cédula

**Commit:** `[pendiente]` — `refactor(recibos): beneficiario_id en vez de datos copiados`

### Problema
`recibos` copiaba `nombre_factura` y `nit_ci_factura` al emitir. Un apellido mal
tipeado había que corregirlo en dos lugares —la ficha y cada recibo— y el
comprobante podía quedar diciendo algo distinto del padrón para siempre.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `database/migrations/…_create_recibos_table.php` | `beneficiario_id` FK RESTRICT; se van las dos columnas copiadas |
| `app/Models/Recibo.php` | Relación `beneficiario()` |
| `app/Services/CobrarService.php` | `cobrar()` y `emitirRecibo()` sacan el titular de lo cobrado; `titularDe()` |
| `app/Services/RevisarCupoService.php` | `enviar()` ya no recibe nombre ni CI |
| `app/Exceptions/CobroInvalidoException.php` | `variosTitulares()` |
| `app/Support/ReciboImpreso.php` | Lee el nombre y la cédula de la relación |
| `ReciboController.php` · `CajaController.php` | Payloads y búsquedas por el beneficiario |
| `CobrarRequest.php` · `RegistrarPagoCupoRequest.php` | Se van las dos reglas |
| `recibos/{index,ver}.tsx` · `caja/cobrar.tsx` · `types/caja.ts` | La pantalla muestra el titular; Caja ya no lo pide tipeado |
| `docs/MER.md` · `docs/NOTAS-CODIGO.md` | La decisión, con lo que cuesta |

### Solución
El titular sale de lo que se está cobrando —`$tramite->beneficiario_id`— y no de
un campo tipeado. De ahí sale una regla nueva: un recibo no puede amparar
trámites de dos personas, y `titularDe()` lo rechaza. En Caja, el bloque «Datos
del comprobante» dejó de pedir NIT y nombre: muestra a nombre de quién va a
salir.

**Lo que esto cuesta, y quedó aceptado:** el encabezado del comprobante dejó de
ser inmutable —corregir un apellido cambia los recibos ya entregados— y se
perdió emitir a nombre de un TERCERO. El monto y el concepto se siguen
congelando.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`. Verificado
> antes en una base SQLite descartable, y probado el circuito entero hasta el
> PDF: el recibo sale con «Vega Alaniz de Lozada» y «1115986-1A BN».

---

## Trabajo 15 — Aprobar un aprovechamiento pide confirmación con casilla

**Commit:** `[pendiente]` — `feat(aprovechamientos): confirmación al aprobar`

### Problema
«Aprobar» firmaba de un clic. Es la acción menos reversible del circuito —el
cupo queda ACTIVO, se escribe la fecha de otorgamiento y no hay «des-aprobar»—
y no pedía ninguna confirmación.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/js/components/ui/confirmar-accion.tsx` | Prop `tono`: `afirmativo` pinta la ventana en verde con un tilde |
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | El botón abre la ventana; la casilla declara que se controlaron las boletas. La de RECHAZAR suma su propia casilla |

### Solución
**Rechazar también lleva casilla**, por lo mismo: es la otra mitad de la firma y
se confirma igual. Ya tenía el motivo obligatorio; ahora además hay que marcar
que el expediente vuelve a ventanilla con ese motivo, registrado a nombre de
quien lo rechaza.

Se reutiliza `ConfirmarAccion` con su casilla de consentimiento. La frase no es
«¿está seguro?» —eso se marca sin leer— sino la declaración que la firma
significa: «Verifiqué las boletas contra el extracto del banco y el expediente
está completo». La ventana dice además qué queda habilitado: cuántos kilos y
hasta cuándo.

El `tono` es nuevo porque el triángulo rojo de las ventanas destructivas decía
«vas a romper algo» justo donde el operador está autorizando.

---

## Trabajo 16 — El estado «Activo» del cupo pasa a «Aprobado», y la autorización se imprime desde el listado

**Commit:** `[pendiente]` — `feat(aprovechamientos): estado Aprobado e impresión desde el listado`

### Problema
Dos cosas del listado de cupos:

1. La columna ESTADO decía «Activo». En este circuito el estado no dice «está
   andando»: dice que ALGUIEN LO FIRMÓ, que es lo que el operador busca ahí.
2. Para imprimir la Autorización de Pesca había que entrar a la ficha, aunque es
   el papel que la persona viene a buscar al mostrador.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Enums/EstadoAprovechamiento.php` | `case Activo = 'activo'` → `case Aprobado = 'aprobado'` |
| `AprovechamientoPesq.php` · `Beneficiario.php` · `RevisarCupoService.php` · `EmitirFaenaService.php` | Las cuatro referencias |
| `resources/js/types/index.d.ts` | El espejo del enum |
| `resources/js/pages/panel/aprovechamientos/index.tsx` | Botones de imprimir: la autorización en las filas firmadas, el recibo desde que se presentó |
| `AprovechamientoController.php` · `types/aprovechamientos.ts` | `recibo_id` y `recibo_numero` en el resumen, con el recibo precargado |
| `docs/MER.md` | El diagrama de estados, que además estaba viejo |

### Solución
Se renombró el CASO y el VALOR, no solo la etiqueta: con `Activo => 'Aprobado'`
el código y la pantalla dirían cosas distintas. Como el valor se guarda en la
columna, hace falta rearmar la base.

El botón de imprimir la autorización aparece con `ya_fue_aprobado` y el permiso
`aprovechamientos.imprimir`, y abre pestaña aparte porque lo que vuelve es un
PDF.

**Y al lado, el RECIBO**, que existe desde el envío: sale en revisión y sigue
después. Para no hacer una consulta por fila —`Pagable::recibos()` es una
consulta, no una relación— el listado precarga `pagos` con su recibo y
`reciboDe()` lo resuelve de ahí; si no está precargado, cae a la consulta.

De paso se corrigió el diagrama de estados del MER, que seguía mostrando
`PENDIENTE ──[se cobra]──▶ ACTIVO` de antes del circuito de revisión.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`. Los cupos
> guardados con `'activo'` no los entiende el enum nuevo. Verificado en una base
> SQLite descartable, circuito entero hasta aprobar: `estado=aprobado`,
> `habilita=true`, `puedeEmitirFaena=true`.

---

## Trabajo 17 — Registrar los depósitos también se confirma

**Commit:** `[pendiente]` — `feat(aprovechamientos): confirmación al registrar depósitos`

### Problema
«Registrar N depósito(s) y enviar a revisión» hacía DOS cosas de un clic, y la
segunda cierra la puerta: sale el recibo numerado —que la persona se lleva— y el
cupo deja de poder editarse o eliminarse. Sin confirmación, un monto mal tipeado
se descubría con el papel ya impreso.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/js/pages/panel/aprovechamientos/ver.tsx` | El submit abre `ConfirmarAccion`; el post se mudó a `registrarDepositos()` |

### Solución
La ventana repite lo que se va a cargar —cuántas boletas y por cuánto— para
contrastarlo con los papeles que están sobre el mostrador, y avisa qué pasa
después. Cuando además envía a revisión pide marcar que los montos y los
números coinciden con los comprobantes del banco; cuando solo registra (el
usuario no tiene el permiso de enviar) no hay casilla.

Se cierra también **al fallar**: los errores del servidor se pintan sobre el
formulario y con la ventana encima no se verían.

---

## Trabajo 19 — La faena cuelga del CARNET y de nada más

**Commit:** `[pendiente]` — `refactor(faenas): se va aprovechamiento_id de permisos_faena`

### Problema
`permisos_faena` guardaba **dos** claves: `aprovechamiento_id` —de dónde salen
los kilos— y `carnet_id` —quién los extrae—. Nada obligaba a que coincidieran:
una faena podía descontar de un cupo distinto del que respalda su propio carnet,
y ninguna restricción lo impedía.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `database/migrations/…_create_permisos_faena_table.php` | Se va la columna y su FK; el único pasa a `(carnet_id, numero_faena)` y queda un solo índice `(carnet_id, estado)` |
| `app/Models/PermisoFaena.php` | Sin la relación; `cupo()` lo alcanza por el carnet; `scopeDelCupo()` va por `whereHas` |
| `app/Models/AprovechamientoPesq.php` | `faenas()` pasa a **`hasManyThrough`** por `carnets`; se fue `siguienteNumeroFaena()` |
| `app/Models/Carnet.php` | `siguienteNumeroFaena()`: el talonario es del carnet |
| `app/Services/EmitirFaenaService.php` | Crea sin la columna; el repetido se busca en el carnet; `completar()` toma el cupo del carnet |
| `FaenaController.php` · `BeneficiarioController.php` | `withMax` sobre el carnet; la ficha usa `cupo()` |
| `EmitirFaenaRequest.php` · `docs/MER.md` | El único que se nombra en el comentario y en la doc |

### Solución
El cupo se alcanza por el camino que ya existía: `carnets.aprovechamiento_id`.
`AprovechamientoPesq::faenas()` es ahora un `hasManyThrough` por `carnets`, así
que `withSum` y `withMax` siguen funcionando y el saldo se sigue calculando con
una sola consulta — nada de N+1.

**El correlativo se mudó con la clave.** El único es `(carnet_id, numero_faena)`,
así que el número de la próxima hoja lo da `Carnet::siguienteNumeroFaena()`, no
el cupo. Consecuencia a tener presente: si una persona renueva el carnet a mitad
de un cupo vigente, el talonario del carnet nuevo arranca otra vez en 1.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`.
>
> Probado en una base SQLite descartable, circuito entero: cupo de 300 kg
> aprobado → carnet con ese cupo → faena 1 de 120 kg (saldo 180) → faena 2 de
> 180 kg (saldo 0, cupo AGOTADO) → `faena->cupo()` devuelve el cupo correcto y
> `PermisoFaena::delCupo(1)` cuenta las dos.

---

## Trabajo 20 — Las reglas de negocio quedan escritas, y en un solo lugar

**Commit:** `[pendiente]` — `docs: REGLAS-NEGOCIO.md`

### Problema
Las reglas del negocio vivían repartidas entre los comentarios del código, el
MER y la cabeza del responsable. No había un documento que dijera **qué tiene
que hacer el sistema**, independiente de cómo está hecho hoy.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `docs/REGLAS-NEGOCIO.md` | Nuevo: los seis pasos, tal como los definió el responsable |
| `CLAUDE.md` | Primera fila de la tabla «qué leer»: es la especificación |

### Solución
Se guardan los seis pasos textuales —beneficiario, aprovechamiento, carnet,
faenas, guías y el ciclo financiero— más un diagrama del flujo completo.

**El documento manda sobre el código**, y por eso termina con una tabla de lo
que todavía no se cumple: hoy hay UNA diferencia, el paso 5. La guía se vincula
al beneficiario (`beneficiario_com_id`) y la regla dice que va al **carnet**.
Es la misma operación que ya se hizo con `permisos_faena`.

---

## Trabajo 21 — Carnets, faenas y guías: las reglas del documento, hechas cumplir

**Commit:** `[pendiente]` — `feat: coherencia tipo_actor/tipo_carnet y la guía cuelga del carnet`

### Problema
Tres huecos entre [REGLAS-NEGOCIO.md](../../REGLAS-NEGOCIO.md) y el código:

1. **Nada ataba el tipo de carnet a la actividad.** `tipos_carnet` no sabía para
   qué actor servía, así que se emitía un «Carnet Comercializador» marcado como
   pescador: el plástico decía una cosa y la base otra.
2. **La guía guardaba su propio beneficiario** (`beneficiario_com_id`), aparte
   del carnet. Con dos claves, nada obligaba a que la persona de la guía fuera
   la titular de su propio carnet.
3. **Un comercializador podía recibir un cupo** si el id viajaba en la petición:
   el servicio lo ignoraba en silencio en vez de rechazarlo.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `…_create_tipos_carnet_table.php` · `TipoCarnet` · `CatalogoSeeder` | Columna `tipo_actor`, con su cast y sembrada |
| `…_create_guias_movimiento_table.php` | `beneficiario_com_id` → `carnet_id`; índice `(carnet_id, estado)` |
| `GuiaMovimiento` | `carnet()`; `comercializador()` pasa a ser método; `scopeDeComercializador` por `whereHas` |
| `Beneficiario` | `guias()` pasa a **`hasManyThrough`** por `carnets` |
| `EmitirGuiaService` · `GuiaController` · `CajaController` · `ReciboController` | Guardan y leen por el carnet |
| `EmitirCarnetService` · `CarnetInvalidoException` | Coherencia tipo/actor, cupo elegido comprobado, comercializador con cupo rechazado |
| `EmitirCarnetRequest` · `CarnetController` | `tipo_carnet_id` se valida contra el actor; `aprovechamiento_id` nullable y `prohibitedIf` comercializador |
| `EmitirFaenaRequest` · `EmitirGuiaRequest` | Los topes de 30 y 5 días, desde las constantes del modelo |
| `PermisoOperativoException` | **Bug:** el mensaje decía que el carnet de pescador sirve para emitir guías |
| `TipoCarnetController` · `tipos-carnet.tsx` · `carnets/crear.tsx` · `types/*` | La actividad en el catálogo, y el formulario filtra los tipos |
| `docs/MER.md` · `docs/REGLAS-NEGOCIO.md` | Las dos tablas y el estado de la implementación |

### Solución
**El catálogo de tipos lleva su propio `tipo_actor`.** Es contra esa columna que
se comprueba la coherencia, nunca contra el nombre —que la unidad edita—. El
formulario de emisión filtra la lista con ella y limpia el tipo elegido si se
cambia de actividad; el servidor lo exige igual, en el Request y en el servicio.

**La guía cuelga del carnet**, exactamente como se hizo con `permisos_faena`. La
asociación se sigue copiando y no es una inconsistencia: es el aval IMPRESO, y
un cambio de gremio no puede reescribir un papel entregado.

**Los topes de días se validan desde las constantes del modelo.** No se podían
violar por arriba —el servicio calcula el vencimiento— pero sí por abajo: una
fecha de salida de hace dos meses emitía una faena nacida vencida.

**El cupo del carnet ahora puede venir elegido** (`aprovechamiento_id`,
opcional). Si viene, se comprueba que sea de esa persona y esté en curso; si es
un comercializador, se rechaza.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`.
>
> Probado en una base SQLite descartable: carnet de comercializador con
> `aprovechamiento_id = NULL`; tipo cruzado rechazado; comercializador con cupo
> rechazado; carnet de pescador con su cupo; guía emitida desde el carnet, con
> 5 días exactos de vigencia y el comercializador leído por la relación;
> pescador intentando emitir guía y comercializador intentando emitir faena,
> los dos rechazados con el mensaje correcto.

---

## Trabajo 22 — El tipo de embarcación pasa a ser obligatorio

**Commit:** `[pendiente]` — `feat(aprovechamientos): tipo_embarcacion obligatorio`

### Problema
El campo era opcional —«si no la declara, déjelo vacío»— con el argumento de que
el talonario de papel tampoco lo marca. El resultado era que la **Autorización
de Pesca** que imprime el sistema salía con «No declarada» en ese renglón, y
alguien lo terminaba llenando a mano.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `…_create_aprovechamientos_pesq_table.php` | La columna deja de ser `nullable` |
| `OtorgarCupoRequest` | `required`, con su mensaje; el `trim` ya no convierte a NULL |
| `OtorgarCupoService` | El parámetro deja de ser opcional en `otorgar()` y `editar()` |
| `AprovechamientoController` | Los dos llamados, con el orden nuevo |
| `crear.tsx` · `editar.tsx` · `ver.tsx` · `types/aprovechamientos.ts` | Marcado obligatorio; se fue el «No declarada» |

### Solución
Obligatorio en los tres niveles: el formulario lo marca, el Request lo exige y
la columna es NOT NULL. Sigue siendo **texto libre** —no hay padrón de
embarcaciones— así que se escribe «canoa», «peque-peque» o «bote» según con qué
llegue la persona.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`. Probado en
> una base descartable: se otorga con «Peque-peque», y un INSERT sin la columna
> lo rechaza la propia base.

---

## Trabajo 23 — Emitir carnet: un solo campo, y se ve de qué cupo cuelga

**Commit:** `[pendiente]` — `feat(carnets): la actividad sale del tipo y el cupo se muestra`

### Problema
Dos cosas del formulario de emisión:

1. **«Actividad que habilita» y «Tipo de carnet» pedían la misma respuesta.**
   Desde que cada tipo del catálogo declara su `tipo_actor`, elegir el tipo ya
   decide la actividad: el operador tenía que acertar dos veces lo mismo, y
   equivocarse en una era un error de validación.
2. **No se veía de qué aprovechamiento iba a colgar el carnet.** El servidor
   elegía solo el cupo vigente de la persona, y la pantalla no decía cuál —ni
   con cuántos kilos— aunque ese volumen es el que se imprime en el plástico.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `carnets/crear.tsx` | Se fue el select de actividad; el de tipo va agrupado por actividad y el cupo se muestra en una tarjeta |
| `CarnetController` | `store()` lee la actividad del tipo; `create()` manda el cupo dentro de la persona |
| `BeneficiarioController` | `cuposElegibles()`, compartido con el buscador; el autocompletado trae las bolsas madre en curso |
| `EmitirCarnetRequest` | Sin `tipo_actor`; el `prohibitedIf` del cupo mira el tipo elegido |
| `types/carnets.ts` · `types/beneficiarios.ts` | `CupoVigente` completo y `cupo_vigente` en la persona sugerida |

### Solución
**La actividad la dice el tipo.** El `<select>` agrupa los tipos con `optgroup`
—Pescador / Comercializador— así que la actividad se sigue viendo, pero se
elige una sola vez. `carnets.tipo_actor` se sigue guardando: es la copia
congelada de la regla, y no se vuelve a leer del catálogo.

**El cupo viaja con la persona.** `cuposElegibles()` es el mismo dato para la
persona preseleccionada y para la elegida con el buscador —antes el aviso de «no
tiene cupo» solo valía para la primera—. La pantalla muestra escala, texto de la
resolución, saldo, estado y vencimiento, y el formulario **manda el
`aprovechamiento_id` explícito** en vez de dejar que el servidor adivine.

**Devuelve una LISTA, y con dos o más aparece un selector.** La regla de una
bolsa por persona deja una sola en curso, así que hoy es siempre una —y con una
no se pregunta: se muestra, porque un desplegable de una opción es un clic que
no decide nada—. Si alguna vez hubiera dos, elige el operador y no el servidor.

**Y se corrigió una inconsistencia entre las dos mitades:** el servicio acepta
un cupo PENDIENTE o EN REVISIÓN para respaldar el carnet —el plástico se emite
con el cupo sin cobrar y los dos se pagan juntos— pero la pantalla solo miraba
los aprobados, así que avisaba «no tiene cupo vigente» sobre alguien que sí
podía sacarlo. Ahora las dos usan el mismo criterio (`enCurso()`), y cuando el
cupo todavía no autoriza a pescar la tarjeta lo dice con su estado a la vista.

---

## Trabajo 24 — La cédula salía sin complemento ni expedido en los listados

**Commit:** `[pendiente]` — `fix: complemento y expedido en los selects de beneficiario`

### Problema
En el listado de aprovechamientos —y en los de carnets, faenas y caja— la
cédula salía pelada: «1234567» en vez de «1234567-1A BN». La ficha, en cambio,
la mostraba completa.

La causa es la trampa de pedir columnas sueltas en un `with()`:
`documento_identidad` concatena `ci`, `complemento` y `expedido`, y el select
traía solo `ci`. Las otras dos volvían NULL y el accesor las descartaba **sin
ningún error**.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `AprovechamientoController` · `CarnetController` · `FaenaController` · `CajaController` | `complemento` y `expedido` en los `with()` (10 selects) |
| `CLAUDE.md` | El caso, en «Trampas conocidas» |

### Solución
Las tres columnas van juntas en cada select. **En la vista PÚBLICA no**: ahí la
cédula se enmascara —solo los últimos tres dígitos— así que traer más columnas
sería exponer de más sin ninguna necesidad.

**Antes:** `1234567` · **Después:** `1234567-1A BN`

---

## Trabajo 25 — La ficha completa de la asociación, en una columna JSON

**Commit:** `[pendiente]` — `feat(asociaciones): columna datos (json) con la ficha del gremio`

### Problema
De una asociación solo se guardaban el nombre y la sigla. Todo lo demás
—personería jurídica, representante legal, teléfono, dirección— vivía fuera del
sistema, en una carpeta.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `…_create_asociaciones_table.php` | Columna `datos` (json, nullable) |
| `app/Models/Asociacion.php` | `CAMPOS` (la lista cerrada de claves), cast a array, `dato()` y `fichaCompleta()` |
| `GuardarAsociacionRequest` | Valida la ficha y descarta claves desconocidas y vacías |
| `AsociacionController` | Manda la ficha y sus rótulos; busca también dentro del JSON |
| `catalogos/asociaciones.tsx` · `types/catalogos.ts` | El bloque «Ficha de la asociación» y el resumen en el listado |
| `docs/MER.md` · `CLAUDE.md` | La decisión y la trampa del LIKE |

### Solución
**Un JSON y no ocho columnas** porque nada del sistema DECIDE con estos datos:
se guardan, se muestran y se imprimen. Sumar el noveno es una línea en
`Asociacion::CAMPOS` —el formulario lo dibuja solo— y no una migración.

**Pero las claves son una lista cerrada.** `CAMPOS` es lo que la pantalla
dibuja y lo que el Request deja pasar; lo que llegue de más del navegador se
descarta, y los campos vacíos no se guardan. Sin eso, un JSON abierto termina
con «telefono», «teléfono» y «tel» en la misma tabla.

**La trampa que apareció al probarlo:** buscar «Pérez» en el catálogo devolvía
CERO. Laravel guarda el JSON con las tildes escapadas a `\uXXXX`, así que el
`LIKE` no encontraba nada —en silencio— justo con los apellidos de acá. El
término se escapa igual antes de comparar. Comprobado con «Pérez», «Juan»,
«71234567» y «PJ-123/2019»: los cuatro encuentran.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`.

---

## Trabajo 26 — Tabla `departamentos`, y el «expedido» del beneficiario apunta a ella

**Commit:** `[pendiente]` — `feat: catálogo cerrado de departamentos`

### Problema
El lugar de expedición de la cédula era texto suelto validado contra una lista
del archivo de configuración. Nada en la base impedía guardar «ZZ», y el nombre
completo del departamento no existía en ningún lado consultable.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `…_create_departamentos_table.php` | Nueva: `codigo` único + `nombre`, **sembrada en la propia migración** |
| `…_create_beneficiarios_table.php` | `expedido` (texto) se va; entra `departamento_id` FK |
| `app/Models/Departamento.php` | Nuevo: `etiqueta()` y `opciones()`. Sin controlador ni rutas |
| `app/Models/Beneficiario.php` | Relación `departamento()` |
| `GuardarBeneficiarioRequest` | Valida contra la tabla, no contra el config |
| `BeneficiarioController` | El desplegable sale de la tabla |
| `config/jichi.php` | Anotado: la lista quedó como SEMILLA, ya no se lee en ejecución |
| `docs/MER.md` | La tabla nueva y la FK |

### Solución
**Sin pantalla, y sembrada en la migración.** Es un catálogo cerrado —no se
agrega un departamento— así que no tiene CRUD; y va en la migración y no en un
seeder porque la tabla tiene que existir llena aunque nadie corra `db:seed`, o
no se puede cargar ni una ficha.

**La ficha guarda `departamento_id` y nada más** —sin copia del código—, a
pedido. Lo que eso obligaba a resolver es que `documento_identidad` arma
«1234567-1A BN» en cada fila de cada listado: con la relación habría que
acordarse de `with('beneficiario.departamento')` en trece consultas, y el día
que alguien lo olvide aparece un N+1 en silencio.

La salida fue `Departamento::codigoDe()`, un mapa **memorizado** de las nueve
filas: se leen una vez por petición y se guardan. Medido: **20 fichas, 1
consulta**. La relación `departamento()` queda para cuando hace falta el nombre
completo, y `Beneficiario::expedido` es ahora un accesor —no una columna— para
que el código siga siendo legible desde el modelo.

**Sin `timestamps` ni `softDeletes`**, al revés que el resto del dominio: no es
una fila que alguien cargue, corrija o dé de baja.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`. Probado:
> los nueve quedan cargados, `1153184-1A LP → La Paz`, un `departamento_id`
> inventado lo rechaza la FK, y veinte fichas resuelven su código con **una
> sola consulta**.

---

## Trabajo 27 — El carnet se cobra y se firma como el aprovechamiento, y lleva sus dos respaldos

**Commit:** `[pendiente]` — `feat(carnets): circuito de revisión, cobro y adjuntos`

### Problema
El carnet nacía ACTIVO y se podía imprimir en el acto, sin haber cobrado nada.
El aprovechamiento ya tenía su circuito —pendiente → en revisión → aprobado, con
recibo y control de boletas— y el carnet, que se cobra igual, no. Y los dos
papeles que la persona trae al mostrador —su cédula y la carta del gremio— no
quedaban en ningún lado.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Enums/EstadoCarnet.php` | `Pendiente` y `EnRevision`, con los cuatro métodos del circuito |
| `…_create_carnets_table.php` | `archivo_ci`, `archivo_asociacion`; el estado nace en `pendiente` |
| `app/Models/Carnet.php` | `admitePagos()`, `puedeEnviarseARevision()`, `puedeRevisarse()`, `yaFueAprobado()` |
| `app/Services/RevisarCarnetService.php` | **Nuevo**: enviar, aprobar y rechazar, espejo del de cupos |
| `app/Services/EmitirCarnetService.php` | Nace pendiente y guarda los dos adjuntos |
| `app/Exceptions/CarnetInvalidoException.php` | Los cuatro mensajes del circuito |
| `CarnetController` | `pagar()`, `enviar()`, `aprobar()`, `rechazar()`; la ficha manda pagos, recibo y banderas |
| `CarnetImpresionController` | El plástico no sale de un carnet sin firmar |
| `routes/panel.php` · `RolSistema` | Rutas y permisos `carnets.enviar` / `carnets.aprobar` |
| `RegistrarPagoCupoRequest` → `RegistrarDepositosRequest` | Renombrado: lo usan los dos trámites |
| `components/panel/pagos/tarjeta-pagos.tsx` | **Nuevo**: la tarjeta de cobro, compartida |
| `carnets/{crear,ver}.tsx` · `types/*` | Los adjuntos, los botones del circuito y la tarjeta |
| `EmitirCarnetService::editar()` / `eliminar()` | Corregir y eliminar el borrador |
| `EditarCarnetRequest` · `EliminarCarnetRequest` | **Nuevos** |
| `carnets/editar.tsx` | **Nueva**: la pantalla de corrección |
| `RolSistema` · `routes/panel.php` | Permisos `carnets.editar` y `carnets.eliminar` |

### Solución
**El mismo circuito, no uno parecido.** `RevisarCarnetService` es el espejo de
`RevisarCupoService`, incluidas las tres condiciones para firmar: estado,
arancel cubierto y **ninguna boleta sin validar**.

**La tarjeta de pagos se extrajo a un componente compartido**, con todo lo que
aprendimos hoy: monto vacío, número solo dígitos, cubrir el monto entero,
confirmación con casilla y el recibo arriba del detalle. Escrita dos veces, el
día que cambie una regla se corrige en una y la otra sigue cobrando con la
vieja.

**Los adjuntos se suben ANTES de la transacción** y el `catch` los borra: una
transacción deshace filas, no escrituras en disco. El tope de 3 MB lo pone
`StorageController`; el Request lo repite para que el mensaje diga qué pasó.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`. Hay dos
> permisos nuevos, así que si la base no se rearma va también
> `php artisan db:seed --class=RolPermisoSeeder`.
>
> Probado el circuito entero en una base descartable: carnet emitido PENDIENTE;
> faena rechazada mientras no esté firmado; depósito parcial rechazado; enviado
> a revisión con su recibo (REC-2026-0002); aprobación frenada por una boleta
> sin validar; y con todo en regla, carnet ACTIVO y faena emitida descontando
> del cupo.

**Y el borrador se corrige y se elimina**, como el del cupo y solo en PENDIENTE:
`EstadoCarnet::permiteEdicion()` / `permiteEliminacion()`, más
`Carnet::puedeEditarse()` y `puedeEliminarse()`, que miran ADEMÁS que no haya
entrado un peso —con un depósito cargado, cambiarle el tipo le cambiaría el
arancel por debajo a algo que alguien ya pagó— y, para eliminar, que no haya
emitido faenas ni guías.

Lo que NO se corrige es el TITULAR ni la ACTIVIDAD: eso sería otro carnet, y el
código impreso lleva el prefijo de la actividad. Para eso se elimina este y se
registra el correcto. Los adjuntos son opcionales al corregir: lo normal es no
volver a subir lo que ya está.

**El botón pasó a decir «Registrar carnet»**: lo que se crea es un expediente
pendiente, no una credencial entregada.

**Y el listado quedó como el de cupos**: foto del titular junto al nombre y la
cédula, y los tres botones de acción —ver, editar y eliminar— con editar y
eliminar apareciendo solo sobre el borrador.

> Ojo con eso último: `puedeEliminarse()` pregunta si hay faenas y guías, que
> son DOS consultas por fila —sesenta en un listado de treinta, invisibles,
> solo para decidir si se dibuja un botón—. El listado agrega
> `withCount(['faenas', 'guias'])` y el modelo reusa los conteos si vinieron.
> Medido: el listado entero resuelve las dos banderas en **una** consulta.

**Pendiente, y conviene hacerlo pronto:** la ficha del cupo sigue con su
formulario de pagos escrito adentro. Hay que pasarla a `TarjetaPagos` para que
quede uno solo.

---

## Trabajo 28 — Los dos listados muestran primero lo último registrado

**Commit:** `[pendiente]` — `fix(listados): ordenar por fecha de carga, no por la de solicitud`

### Problema
`/panel/carnets` y `/panel/aprovechamientos` ordenaban por `fecha_solicitud`, que
es un DÍA declarado por el operador y puede ser pasado —se usa para poner al día
lo que se tramitó en papel—. Un expediente cargado recién con fecha de marzo
aparecía al fondo, y como además todos los del día comparten la misma fecha, el
desempate quedaba librado al orden que devolviera el motor: lo recién cargado no
estaba donde se lo busca, que es arriba.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `AprovechamientoController::index()` | `orderByDesc('aprovechamientos_pesq.created_at')` + `id` |
| `CarnetController::index()` | `orderByDesc('carnets.created_at')` + `id` |

### Solución
Se ordena por `created_at`, que es el momento real de la carga y es justo lo que
muestra la columna «Registrado» que se agregó hoy en los dos listados: lo que se
ve arriba coincide con lo que dice esa celda.

El `id` desempata. Sin él, dos filas cargadas en el mismo segundo pueden salir
en distinto orden en cada consulta, y con paginado eso hace que una fila se
repita en la página 2 y otra no aparezca nunca.

Las columnas van CALIFICADAS con su tabla: los dos listados filtran por `estado`
y ya lo calificaban por eso mismo —`carnets`, `tramites` y `rubros` tienen todas
esa columna y los reportes las cruzan con `join`—.

> Verificado sobre la base de trabajo: en carnets, con las dos filas con la
> misma `fecha_solicitud`, sale primero la id=2 (registrada 03:13) y después la
> id=1 (02:52).

---

## Trabajo 29 — La barra de filtros del listado de carnets, igual a la de los demás

**Commit:** `[pendiente]` — `style(carnets): la barra de filtros del padrón`

### Problema
El listado de carnets tenía su propia barra: un `flex` con el buscador primero,
los tres selectores después y un botón con la lupa al final. Beneficiarios y
aprovechamientos usan otra —«Mostrar N registros» a la izquierda y los filtros
pegados al buscador a la derecha—, así que cada pantalla se manejaba distinto.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `resources/js/pages/panel/carnets/index.tsx` | La barra pasa a la grilla de doce columnas del padrón |

### Solución
La misma estructura de `aprovechamientos/index.tsx`: grilla de doce columnas,
«Mostrar N registros» a la izquierda y, a la derecha, actividad + estado +
buscador en un solo grupo. La lupa va DENTRO del campo y el buscador es un
`<form>` propio para que el Enter lo envíe; los selectores filtran al cambiar,
así que el botón suelto sobraba.

Carnets tiene un filtro más que cupos —la actividad—, así que las columnas van
3/9 en vez de 4/8: con 4 el grupo de la derecha rompía a dos renglones en una
pantalla mediana.

---

## Trabajo 30 — La misma barra de filtros en los cinco listados que faltaban

**Commit:** `[pendiente]` — `style(panel): una sola barra de filtros en todos los listados`

### Problema
Cada listado tenía su propia barra. Faenas, guías, caja y recibos usaban un
`flex` con el buscador primero y un botón con la lupa al final; asociaciones
tenía el buscador metido en el encabezado de la tarjeta y **ni siquiera
paginaba** —traía el catálogo entero con un `get()`—. Seis pantallas, tres
disposiciones distintas.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `faenas/index.tsx` · `guias/index.tsx` | La barra pasa a la grilla de doce columnas |
| `caja/index.tsx` · `recibos/index.tsx` | Ídem; el rango de fechas va en el grupo derecho |
| `catalogos/asociaciones.tsx` | Barra nueva, filtro de estado y paginador |
| `AsociacionController::index()` | `paginate()` + `withQueryString()`, filtro de estado y `opcionesPorPagina` |

### Solución
La misma estructura del padrón de beneficiarios: «Mostrar N registros» a la
izquierda y, a la derecha y juntos, los filtros propios de cada pantalla más el
buscador con la lupa DENTRO del campo. El botón suelto de la lupa desapareció:
los selectores filtran al cambiar y el buscador es un `<form>` propio, así que
el Enter ya lo envía.

Las columnas se reparten según cuántos filtros tenga cada listado: 4/8 con uno
—faenas, asociaciones— y 3/9 con dos —guías (estado y origen), caja y recibos
(desde y hasta)—. Con 4/8 y dos filtros el grupo derecho rompe a dos renglones
en una pantalla mediana.

**Asociaciones pasó a paginar**, que es lo que faltaba para que la barra
significara algo: sin páginas, «Mostrar 15 registros» no tiene nada que hacer.
El listado se arma con `paginate()->withQueryString()->through(...)` —el
`through` reemplaza al `map`, que sobre un paginador devolvería una colección
suelta y se perdería el paginado— y suma el filtro por estado, que ya existía
como enum y solo usaba el formulario.

> Verificado llamando a los cinco controladores: las cinco pantallas responden
> con `filtros` y `opcionesPorPagina`, y en asociaciones el filtro de estado
> devuelve 4 activas y 0 inactivas, con `per_page` siguiendo al selector.

---

## Trabajo 31 — Los dos catálogos que faltaban, y el alta de tipos de carnet se retira

**Commit:** `[pendiente]` — `feat(catalogos): barra y paginado en escala y tipos; sin alta de tipos`

### Problema
La escala de aprovechamiento y los tipos de carnet eran las dos últimas
pantallas con una barra propia: ni buscador con la disposición del padrón, ni
filtros, ni paginado —las dos traían el catálogo entero con un `get()`—. Y en
tipos de carnet se podían agregar filas desde el panel, cuando la lista sale de
la resolución.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `CategoriaAprovechamientoController::index()` | Buscador, filtro de modalidad y paginado; `siguienteNumero` como prop |
| `TipoCarnetController` | Ídem con el filtro de actividad; **se retira `store()`** |
| `routes/panel.php` | Se retira `POST /panel/catalogos/tipos-carnet` |
| `GuardarTipoCarnetRequest` | Pasa a ser solo de corrección |
| `catalogos/escala.tsx` · `catalogos/tipos-carnet.tsx` | La barra, el paginador y —en tipos— el formulario sin alta |
| `docs/MER.md` | Queda dicho que el catálogo de tipos no se da de alta |

### Solución
La misma barra que el resto: «Mostrar N registros» a la izquierda y, a la
derecha, el filtro propio —modalidad en la escala, actividad en los tipos— junto
al buscador con la lupa dentro del campo.

**El número que sigue y los huecos NO salen de la página.** Al paginar la escala
apareció una trampa fina: `Nuevo tramo` proponía su número con
`Math.max(...escala.map(…)) + 1` sobre las filas dibujadas, así que en la página
2 —o con un filtro puesto— hubiera propuesto un número ya usado, y los huecos de
la escala hubieran aparecido y desaparecido al cambiar de página. El controlador
hace una consulta aparte con TODOS los tramos para esas dos cosas y manda
`siguienteNumero` resuelto. Comprobado: con el listado filtrado a una fila,
sigue diciendo 8.

**El alta de tipos de carnet se retira de verdad.** Sacar el botón no alcanza:
la ruta seguía aceptando un POST armado a mano, y el catálogo es el que decide
qué arancel se cobra. Se fueron el botón, el `store()` y la ruta; el formulario
quedó de corrección —`tipo` ya no es nullable— y `route:list` muestra solo
`index` y `update`.

> Ojo con `precio_bs` en ese formulario: al dejar de ser un alta, el valor
> inicial pasó a venir del modelo y TypeScript lo tipó `number`, que no admite
> lo que devuelve el `<input>`. Va `String(tipo.precio_bs)`.

---

## Trabajo 32 — La faena se cobra y se firma como el carnet y el aprovechamiento

**Commit:** `[pendiente]` — `feat(faenas): circuito de pago, revisión y recibo`

### Problema
La faena nacía ACTIVA: se registraba y en el acto autorizaba la salida, sin que
hubiera entrado un peso ni firmado nadie. Los otros dos trámites —el carnet y la
autorización de aprovechamiento— ya recorrían pendiente → en revisión → aprobado,
con sus depósitos y su recibo; la salida de pesca, que también se cobra, no.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Enums/EstadoFaena.php` | `Pendiente` y `EnRevision`, más los métodos del circuito |
| `…_create_permisos_faena_table.php` | `fecha_solicitud`, `fecha_emision`; el estado nace `pendiente` |
| `app/Models/PermisoFaena.php` | Trait `Pagable`, `montoACobrar()`, las banderas y `motivoSinAutorizar()` |
| `app/Services/RevisarFaenaService.php` | **Nuevo**: enviar, aprobar y rechazar |
| `app/Services/EmitirFaenaService.php` | La faena nace PENDIENTE y guarda el día en que se pidió |
| `app/Exceptions/PermisoOperativoException.php` | Los cuatro mensajes del circuito |
| `config/jichi.php` | `faenas.tarifa_base` (`JICHI_FAENA_TARIFA_BASE`, 30 Bs) |
| `FaenaController` | `pagar()`, `enviar()`, `aprobar()`, `rechazar()`; la ficha manda pagos, recibo y banderas |
| `CobrarService` · `CajaController` · `Beneficiario` | La faena es cobrable también por caja |
| `routes/panel.php` · `RolSistema` | Rutas y permisos `faenas.enviar` / `faenas.aprobar` |
| `RechazarFaenaRequest` | **Nuevo** |
| `faenas/{index,crear,ver}.tsx` · `types/faenas.ts` · `types/index.d.ts` | La tarjeta de pagos, los botones y las columnas |

### Solución
**El mismo circuito, no uno parecido.** `RevisarFaenaService` es el espejo de
`RevisarCarnetService`, con las tres condiciones para firmar: estado, arancel
cubierto y ninguna boleta sin validar. La pantalla usa el mismo `TarjetaPagos` y
los mismos diálogos de confirmación.

**Los kilos se reservan desde el PEDIDO.** `consumeCupo()` deja afuera solo a la
vencida, así que una faena pendiente ya pesa contra la bolsa. Sin eso, tres
solicitudes por el cupo entero pasarían las tres —cada una leería el saldo antes
de que las otras estuvieran escritas— que es exactamente la trampa que ya nos
había mordido cobrando dentro de un `foreach`.

**El titular del recibo llega por un accesor.** `CobrarService` lo pide como
`beneficiario_id`, columna que la faena no tiene: `PermisoFaena::beneficiarioId()`
lo resuelve por el carnet, en vez de duplicar la clave en la tabla —que es lo que
ya se había sacado de `permisos_faena` con `aprovechamiento_id`—.

> ⚠️ **HAY QUE VOLVER A MIGRAR**: `php artisan migrate:fresh --seed`, y si la
> base no se rearma, `php artisan db:seed --class=RolPermisoSeeder` por los dos
> permisos nuevos.
>
> Circuito entero probado en una base descartable: faena PENDIENTE de 30 Bs;
> envío frenado sin cobrar; depósito parcial rechazado; enviada con su recibo
> (REC-2026-0003, concepto «Faena N° 0001 - 50 kg»); aprobación frenada por una
> boleta sin validar; rechazo y reenvío **sin** emitir un segundo recibo;
> aprobada con fecha de emisión; completada a 48,5 kg con el cupo recalculado.
> Y el saldo del cupo baja al registrar una pendiente: 251,5 → 151,5 kg.

**Lo que NO cambió:** completar sigue siendo de ventanilla y sigue exigiendo la
faena aprobada, y la faena se sigue sin editar ni borrar.

---

## Informe para presentación (copiar/pegar en Word)

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Corrección en los cupos de pesca: ahora se emite un solo recibo
por trámite, con el total de todos los depósitos.

**Problema detectado:**
Cuando una persona pagaba su cupo de pesca con más de un depósito bancario, el
sistema le emitía un recibo distinto por cada boleta. En el caso revisado, dos
depósitos de un mismo cupo salieron como dos recibos separados, uno de 330
bolivianos y otro de 82,50.

Eso no corresponde. El cupo de pesca es un trámite: la persona lo solicita, lo
paga y lo presenta en ventanilla. Por ese trámite le corresponde **un** recibo,
que diga el total pagado, sin importar si lo cubrió con una boleta o con cinco.
Además, cada recibo consume un número de la numeración oficial que revisa
Contabilidad, así que la serie avanzaba de a dos o de a tres, y el resumen de
caja del día mostraba varios cobros donde en realidad hubo uno solo.

Al revisarlo apareció una segunda falla que nadie había notado: el control que
impide cobrar más de lo que la persona debe se hacía depósito por depósito, por
separado. Si se cargaban tres depósitos por el monto total cada uno, los tres
pasaban y el trámite quedaba cobrado tres veces sin ninguna alarma.

**Solución aplicada:**
Se separó el momento de cargar la plata del momento de entregar el papel, que es
como funciona en el mostrador:

1. Mientras el trámite está **pendiente**, el operador carga todos los depósitos
   que la persona trajo, cada uno con su boleta, su fecha y su foto o PDF.
   Todavía no se emite ningún recibo.
2. Cuando los depósitos cubren el monto y el operador **envía el trámite a
   revisión**, el sistema emite **un solo recibo** con el total y le asocia todos
   los depósitos. Ese es el papel que la persona se lleva.
3. Si el trámite se rechaza y se vuelve a presentar, **no se emite un recibo
   nuevo**: sigue valiendo el que la persona ya tiene en la mano.

También se corrigió el control de montos: ahora, al revisar cada depósito, el
sistema descuenta lo que ya suman los depósitos anteriores del mismo envío. Si
entre todos se pasan de lo que se debe, no se guarda nada y se avisa.

En la pantalla del cupo, el número de recibo dejó de repetirse en cada fila de la
lista de depósitos y ahora aparece una sola vez arriba, con el total y el botón
para imprimirlo. Mientras el trámite está pendiente, ese recuadro avisa que el
recibo se emite al enviarlo a revisión.

**Detalle de archivos modificados:**
1. Base de datos: el depósito ahora puede existir antes que el recibo.
2. Servicio de caja: carga los depósitos sin emitir papel, y emite un único
   recibo con todos ellos cuando corresponde.
3. Servicio de revisión: al enviar el trámite, emite el recibo en la misma
   operación que el cambio de estado, para que nunca quede uno sin el otro.
4. Pantalla del cupo de pesca y listado de caja: muestran el recibo del trámite
   una sola vez y avisan cuando todavía no se emitió.

**Resultado:** Cada trámite de aprovechamiento pesquero entrega un único recibo
con el total pagado, la numeración oficial deja de gastarse de más, el resumen de
caja refleja los cobros reales del día, y ya no es posible cobrar de más
partiendo el pago en varios depósitos.

---

**Informe de trabajo (2) — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Se incorporó el control de las boletas de depósito mientras el
trámite está en revisión, con el registro de quién las validó y en qué momento.

**Problema detectado:**
Una vez que el trámite se presentaba, quien tenía que firmarlo podía aprobarlo
directamente, sin que nadie hubiera comparado las boletas contra el estado de
cuenta del banco. El sistema solo verificaba que la suma de los depósitos
alcanzara el monto, es decir, que los números CARGADOS dieran bien — no que esa
plata hubiera entrado de verdad.

Tampoco quedaba registrado quién había revisado cada boleta ni cuándo, así que
ante un reclamo posterior no había forma de saber quién respondió por ese dinero.
Y si una boleta no cuadraba, la única salida era rechazar el trámite completo y
devolverlo a ventanilla por un solo número mal copiado.

**Solución aplicada:**
Mientras el trámite está EN REVISIÓN, cada depósito se puede:

1. **Validar** — la boleta coincide con el estado de cuenta del banco. Queda
   guardado el nombre de quien la validó y la fecha y hora exactas.
2. **Observar** — no coincide, y hay que escribir por qué. Ese texto es lo que
   ventanilla va a leer para saber qué corregir. El depósito sigue contando en el
   saldo: lo que está en duda es si la boleta respalda lo que dice, no que el
   dinero esté.
3. **Corregir** — es lo que hace ventanilla con un depósito observado: arreglar
   el monto, el número de boleta, la fecha o el archivo escaneado. Al corregirlo,
   el depósito vuelve a quedar SIN VALIDAR y alguien tiene que mirarlo de nuevo.

Validar y observar son tareas de supervisión; corregir es de ventanilla. Son
permisos separados a propósito: si fueran el mismo, la misma persona objetaría y
resolvería su propia objeción, y el control no serviría de nada.

Además, **el trámite ya no se puede aprobar mientras quede alguna boleta sin
validar u observada**. El botón «Aprobar» aparece apagado e indica cuántas
faltan. Sin esa condición, validar habría sido solo un adorno.

**Detalle de archivos modificados:**
1. Base de datos: cada depósito guarda ahora su situación de control, la
   observación escrita, quién lo cargó, quién lo validó y en qué momento.
2. Servicio de control de pagos (nuevo): las tres operaciones, con las reglas de
   cuándo se puede hacer cada una.
3. Servicio de revisión: la aprobación exige que todas las boletas estén
   validadas.
4. Pantalla del cupo de pesca: columna de validación con su estado, el nombre de
   quien la revisó y la fecha, los tres botones y la ventana para escribir la
   observación o corregir el depósito.

**Resultado:** Ninguna autorización de pesca se firma sin que alguien haya
comparado cada boleta contra el banco, queda registrado por escrito quién
respondió por cada depósito y cuándo, y un problema en una boleta se resuelve en
la misma pantalla en lugar de devolver el trámite completo.

---

**Informe de trabajo (3) — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Se quitó la casilla de «Efectivo» del recibo oficial impreso.

**Problema detectado:**
El recibo copiaba el renglón del talonario de papel, que ofrece dos formas de
pago: depósito bancario o efectivo. En esta unidad no se cobra en efectivo ni por
QR —todo pago es un depósito bancario—, así que esa casilla salía vacía en todos
los recibos emitidos. Una opción impresa que nunca se puede marcar solo genera la
pregunta de por qué no se usa.

**Solución aplicada:**
El renglón quedó con el depósito bancario marcado y el número de boleta, que es
el dato que se compara contra el estado de cuenta del banco. El espacio que
ocupaba la casilla de efectivo se lo lleva ese número, que antes se quedaba
corto cuando la boleta traía muchos dígitos.

**Detalle de archivos modificados:**
1. Plantilla del recibo oficial: se quitaron el texto «Efectivo» y su casilla.
2. Generador del recibo: dejó de enviar un dato que siempre valía lo mismo.

**Resultado:** El recibo impreso muestra solo la forma de pago que la unidad
realmente usa, y el número de boleta se lee completo.

---

**Informe de trabajo (4) — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El sistema ahora imprime la Autorización de Pesca para
Aprovechamiento Pesquero.

**Problema detectado:**
Cuando un cupo de pesca quedaba aprobado, el pescador tenía que llevarse la
Autorización de Pesca —el formulario del talonario del SEDAG— y el sistema no la
emitía: se seguía llenando a mano, con el riesgo de copiar mal el nombre, el
volumen autorizado o las fechas de vigencia.

**Solución aplicada:**
Se agregó el formulario al sistema, copiando el papel tal cual: el mismo
encabezado, los mismos renglones, la tabla de tamaños mínimos de cada especie y
las reglas sobre el uso de redes. Sale en una hoja tamaño carta, lista para
imprimir.

El botón aparece en la pantalla del cupo **solo cuando el trámite está
aprobado**: antes de la firma no hay nada que autorizar. Si el cupo ya venció,
igual se puede volver a imprimir, por si hace falta reponer el papel de una
gestión anterior.

El formulario se llena solo con los datos que ya están cargados: nombre,
domicilio, cédula, tipo de embarcación, volumen en kilos, vigencia, valor de la
concesión y la fecha del último depósito.

**Detalle de archivos modificados:**
1. Plantilla del formulario (nueva): el calco del papel.
2. Generador del documento (nuevo): arma el PDF y comprueba que el trámite esté
   aprobado.
3. Pantalla del cupo: el botón «Autorización de pesca».
4. Permisos: se agregó uno propio para entregar este papel, separado del de
   consultar la ficha.

**Resultado:** La autorización sale impresa con los datos del sistema, sin
copiarlos a mano, y solo para los trámites que realmente fueron aprobados.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Los botones de Ver, Editar y Eliminar son iguales en todo el
sistema.

**Problema detectado:**
Cada pantalla mostraba esos tres botones de una forma distinta: en una eran de
color, en otra grises, y en otra el de eliminar salía todo rojo. El personal de
ventanilla tenía que fijarse en el texto de cada botón para saber cuál era cuál,
porque el aspecto no ayudaba, y en un descuido se puede apretar el que borra.

**Solución aplicada:**
Se definió un único estilo, tomado de la pantalla de Cupos de pesca, y se aplicó
a todas las pantallas: el botón de ver es celeste, el de editar es amarillo y el
de eliminar es rojo, siempre con su borde. La misma acción se ve igual en
Beneficiarios, Carnets, Faenas, Guías, Cupos y Catálogos.

**Detalle de archivos modificados:**
1. Botón del sistema: se le agregaron los tres estilos, para que ninguna
   pantalla tenga que inventarse los colores.
2. Pantallas de Cupos, Beneficiarios, Carnets, Faenas y Guías: sus botones pasan
   a usar el estilo común.
3. Pantallas de Catálogos (Asociaciones, Escala y Tipos de carnet): el botón de
   editar de cada listado.

**Resultado:** El color dice qué hace el botón antes de leerlo, en todas las
pantallas por igual, y una pantalla nueva ya no tiene que copiar colores a mano.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** La pantalla del cupo explica el motivo verdadero por el que
todavía no se pueden emitir faenas.

**Problema detectado:**
Al crear un cupo nuevo, la pantalla avisaba que «el cupo está vencido o sin
saldo». Las dos cosas eran falsas: el cupo recién creado vence a fin de año y
tiene todos sus kilos. Lo único que faltaba era cobrarlo y firmarlo. El aviso
hacía perder tiempo buscando un problema inexistente. Además aparecía un
recuadro de «Faenas emitidas» diciendo que no hay ninguna, sobre un cupo que
todavía no puede tener faenas.

**Solución aplicada:**
Ahora el aviso dice lo que realmente pasa: si está pendiente, que falta cubrir
el monto y enviarlo a revisión; si está esperando la firma, que está presentado;
si venció, que venció; y si se quedó sin kilos, que se agotó. El recuadro de
faenas aparece recién cuando el cupo fue aprobado.

**Detalle de archivos modificados:**
1. Cupo de pesca (reglas): ahora sabe decir por qué no autoriza faenas, no solo
   que no autoriza.
2. Pantalla del cupo: muestra ese motivo y oculta el recuadro de faenas hasta
   que el cupo esté firmado.
3. Barra de volumen: mientras el cupo no esté aprobado dice «200 kg
   solicitados» en vez de mostrar consumido, disponible y porcentaje usado, que
   son datos de un cupo que ya está funcionando.
4. Ficha del beneficiario: sus cupos siguen el mismo criterio.

**Resultado:** El operador lee qué le falta hacer —cobrar y enviar a revisión—
en vez de un aviso que no correspondía, y la pantalla no muestra consumos ni
saldos de un cupo que todavía no empezó a usarse.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Se corrigió el formulario para cargar los depósitos de un cupo.

**Problema detectado:**
Al presionar «Agregar pago», el monto aparecía ya escrito con el total a pagar.
Un campo que viene lleno se acepta sin leerlo, y el monto tiene que ser el que
dice la boleta del banco: si la persona depositó una parte, se estaba
registrando otra cifra. Además, mientras se llenaba el formulario, más abajo
seguía el cartel «Sin pagos registrados», que hacía dudar de si lo escrito se
estaba guardando.

**Solución aplicada:**
El monto ahora arranca vacío y lo escribe quien atiende, leyendo la boleta. Lo
que falta cobrar se sigue viendo abajo del formulario, donde dice cuánto suman
los depósitos cargados y cuánto queda pendiente. El cartel de «sin pagos»
desaparece mientras el formulario está abierto.

**Detalle de archivos modificados:**
1. Pantalla del cupo: el campo del monto y el cartel de «sin pagos».

**Resultado:** El depósito se registra por lo que dice la boleta, y la pantalla
no contradice lo que el operador está cargando.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El número de transacción de la boleta ahora admite solo números.

**Problema detectado:**
El campo del número de boleta dejaba escribir letras y símbolos. Con un error de
tipeo, el sistema guardaba un número de boleta que no existe, y el control que
impide cargar dos veces la misma boleta dejaba de funcionar: «00123A» y «00123»
pasaban como dos depósitos distintos.

**Solución aplicada:**
El campo acepta únicamente dígitos, en las tres pantallas donde aparece: la
ficha del cupo, Caja y la corrección de un depósito. Se conservan los ceros del
principio, que son parte del número de la boleta. El control también se hace en
el servidor, para que no dependa del navegador.

**Detalle de archivos modificados:**
1. Validaciones de cobro (tres archivos): el número de boleta tiene que ser
   numérico.
2. Pantallas de cupo, Caja y corrección de depósito: el campo no deja tipear
   otra cosa.

**Resultado:** El número de boleta que se guarda es el que figura en el papel, y
el control de boletas repetidas vuelve a ser confiable.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El cupo de pesca ahora guarda por separado la fecha en que se
solicitó y la fecha en que se otorgó.

**Problema detectado:**
Al registrar un cupo nuevo, el sistema anotaba ese día como «fecha de
otorgamiento» y así lo mostraba en la ficha, aunque el trámite recién estuviera
empezando y todavía nadie lo hubiera aprobado. Un trámite puede quedar pendiente
varios días, o incluso ser rechazado, y en el papel la fecha de otorgamiento es
la del día en que se firma la autorización.

**Solución aplicada:**
Ahora son dos fechas. Al registrar el trámite se guarda la fecha de solicitud
—el día que la persona lo pidió, que se puede retroceder para poner al día
trámites de papel—. La fecha de otorgamiento la escribe el sistema sola, el día
que se aprueba, y hasta entonces la ficha no la muestra. El vencimiento se
vuelve a calcular en ese momento, para que un trámite pedido a fin de año y
firmado en enero valga por la gestión nueva.

**Detalle de archivos modificados:**
1. Base de datos: se agregó la fecha de solicitud y la de otorgamiento quedó
   vacía hasta la firma.
2. Registro y corrección del cupo: guardan la fecha de solicitud.
3. Aprobación: escribe la fecha de otorgamiento y recalcula el vencimiento.
4. Pantallas de alta, corrección y ficha del cupo: el campo pasa a llamarse
   «Fecha de solicitud» y la ficha muestra las dos fechas.

**Resultado:** La ficha dice la verdad: cuándo se pidió el cupo y, si ya se
firmó, cuándo se otorgó.

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`) para que el cambio tome efecto.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** La barra de búsqueda del listado de cupos quedó igual a la del
padrón de beneficiarios.

**Problema detectado:**
Las dos pantallas hacen lo mismo —listar y buscar— pero se veían distintas: en
Beneficiarios la barra dice «Mostrar N registros» a la izquierda y tiene el
buscador con la lupa adentro a la derecha; en Cupos eran campos sueltos con un
botón de lupa aparte.

**Solución aplicada:**
Se copió la barra del padrón a la pantalla de cupos. El filtro por estado —que
el padrón no tiene— quedó al lado del buscador, en la derecha, porque los dos
sirven para lo mismo. Se busca escribiendo y apretando Enter, como en la otra
pantalla.

Además la lista ahora muestra tres fechas: cuándo se solicitó el trámite,
cuándo se otorgó —vacía si todavía no se firmó— y cuándo vence.

**Detalle de archivos modificados:**
1. Pantalla de listado de cupos: la barra de arriba de la tabla.

**Resultado:** Las dos pantallas se usan igual, y quien aprendió a buscar en una
ya sabe buscar en la otra.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El cupo de pesca ya no acepta un depósito que no cubra el monto.

**Problema detectado:**
Si el trámite costaba 165 Bs y se cargaba una boleta de 150, el sistema la
guardaba igual. El trámite quedaba cobrado a medias: no se podía enviar a
revisión, no se emitía el recibo y nada avisaba que faltaban 15 Bs salvo entrar
de nuevo a la ficha.

**Solución aplicada:**
Ahora los depósitos se registran todos juntos y tienen que sumar el total. Se
pueden cargar varias boletas —si la persona depositó en dos veces— pero se
guardan en un solo acto. Mientras falte plata, el botón queda apagado y debajo
dice cuánto falta.

El botón apagado es solo una comodidad: alguien con conocimientos puede
reactivarlo desde las herramientas del navegador. Por eso la comprobación se
hace además en el servidor, en dos puntos, y ahí no se puede esquivar.

**Detalle de archivos modificados:**
1. Registro de depósitos del cupo: exige que la suma cubra el monto.
2. Pantalla del cupo: el botón se apaga y explica cuánto falta.

**Resultado:** No quedan trámites cobrados a medias, y quien atiende ve en el
momento qué falta para poder presentar el expediente. Si el depósito fue por más
de lo que costaba el trámite, se registra igual: el excedente queda a favor de la
entidad y el trámite se puede enviar a revisión.

> **Nota:** en la pantalla de Caja el cobro parcial se sigue permitiendo, porque
> ese circuito lo comparten los carnets y las guías. Avisar si también debe
> exigir el total.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El recibo impreso ahora muestra la cédula completa, con el lugar
de expedición.

**Problema detectado:**
En el recibo, junto a «C.I.», salía solo el número de cédula: faltaban el
complemento y el departamento donde fue expedida. En el talonario de papel esos
datos van escritos, y son los que identifican a la persona si alguien tiene que
verificar el comprobante.

**Solución aplicada:**
El recibo toma la cédula completa de la ficha del beneficiario, tal como se
imprime en el carnet: número, complemento y expedido. En Caja, el campo del
comprobante también se propone completo, y se puede cambiar si el recibo sale a
nombre de otra persona o de una empresa con NIT.

**Detalle de archivos modificados:**
1. Emisión del recibo del cupo: copia la cédula completa.
2. Pantalla de Caja: propone la cédula completa en el campo del comprobante.

**Resultado:** El recibo sale con la cédula como la pide el papel. Los recibos ya
emitidos no cambian, porque cada comprobante guarda una copia de los datos con
los que se entregó.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El recibo ahora guarda a qué persona pertenece, en vez de copiar
su nombre y su cédula.

**Problema detectado:**
Cada recibo guardaba el nombre y la cédula escritos en el momento de cobrar. Si
después se corregía un dato en la ficha de la persona —un apellido mal escrito,
por ejemplo—, los recibos seguían mostrando el error, y había que corregirlo uno
por uno.

**Solución aplicada:**
El recibo guarda la referencia a la persona, y el nombre y la cédula se toman de
su ficha cada vez que se imprime. Se corrige en un solo lugar. En la pantalla de
Caja ya no hay que escribir el nombre ni el NIT: el sistema muestra a nombre de
quién va a salir el recibo, tomándolo de los trámites que se están cobrando.

**A tener en cuenta:** como los datos ya no se copian, si se corrige la ficha de
una persona, sus recibos ya entregados van a imprimirse con el dato corregido. Y
un recibo ya no puede emitirse a nombre de otra persona o empresa que pague por
el pescador; si eso hiciera falta, hay que avisar.

**Detalle de archivos modificados:**
1. Base de datos: el recibo guarda la referencia al beneficiario.
2. Emisión de recibos (Caja y cupos): toman la persona de lo que se cobra.
3. Pantallas de Recibos y Caja: muestran el titular tomado del padrón.

**Resultado:** El nombre y la cédula del recibo salen siempre iguales a los de
la ficha, y se corrigen en un solo lugar.

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`).

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Aprobar un cupo de pesca ahora pide una confirmación.

**Problema detectado:**
El botón «Aprobar» firmaba el trámite con un solo clic. Es la acción que no
tiene vuelta atrás: el cupo queda habilitado, se registra la fecha de
otorgamiento y no existe forma de desaprobarlo. Un clic equivocado quedaba hecho.

**Solución aplicada:**
Antes de aprobar se abre una ventana que explica qué va a pasar —cuántos kilos
quedan autorizados y hasta qué fecha— y pide marcar una casilla declarando que
se revisaron las boletas contra el extracto del banco. Hasta que no se marca, el
botón de aprobar no se habilita.

Rechazar quedó igual de exigente: además del motivo por escrito, hay que marcar
una casilla que dice que el expediente vuelve a ventanilla con ese motivo y a
nombre de quien lo rechaza.

**Detalle de archivos modificados:**
1. Ventana de confirmación del sistema: se le agregó una versión en verde, para
   las acciones que autorizan en vez de eliminar.
2. Pantalla del cupo: el botón «Aprobar» abre esa ventana.

**Resultado:** No se aprueba de un clic distraído, y queda claro qué declara
quien firma.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El estado del cupo pasa a llamarse «Aprobado», y la autorización
se imprime desde el listado.

**Problema detectado:**
En la lista de cupos, el estado de los trámites firmados decía «Activo», cuando
lo que corresponde decir es que fue aprobado: es el resultado de la firma. Y
para imprimir la Autorización de Pesca había que entrar al detalle del trámite,
aunque es el papel que la persona viene a buscar.

**Solución aplicada:**
El estado ahora se llama «Aprobado» en todo el sistema. Y en la lista, cada
trámite aprobado tiene un botón de impresora que abre la autorización lista para
imprimir, sin entrar al detalle. Al lado aparece otro botón para reimprimir el
recibo, desde que el trámite se presenta a revisión.

**Detalle de archivos modificados:**
1. Estados del cupo: «Activo» pasa a «Aprobado».
2. Lista de cupos: botón para imprimir la autorización y botón para reimprimir
   el recibo.

**Resultado:** La lista dice lo que realmente pasó con el trámite, y el papel se
imprime desde ahí mismo.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Se cargaron los departamentos de Bolivia como tabla del sistema.

**Problema detectado:**
El departamento donde se expide la cédula se guardaba como un texto libre, y el
sistema no tenía registrados los departamentos: solo una lista escrita en un
archivo de configuración. Nada impedía que quedara guardado un código que no
existe, y el nombre completo del departamento no estaba disponible para
reportes.

**Solución aplicada:**
Se creó la tabla de departamentos con los nueve de Bolivia y su código oficial
—BN para Beni, LP para La Paz, y así—, ya cargados. No tiene pantalla de
administración a propósito: no se agregan ni se quitan departamentos. La ficha
del beneficiario guarda ahora la referencia a esa tabla —ya no el texto del
código— así que solo se puede elegir uno de los nueve y el sistema rechaza
cualquier otro. Donde se imprime la cédula completa, el código se sigue viendo
igual.

**Detalle de archivos modificados:**
1. Base de datos: tabla de departamentos, cargada desde el inicio.
2. Ficha del beneficiario: el lugar de expedición apunta a esa tabla.
3. Formularios de alta y edición: la lista sale de la tabla.

**Resultado:** El lugar de expedición de la cédula ya no puede quedar con un
dato inventado, y el nombre completo del departamento queda disponible para
reportes.

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`).

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El formulario para emitir carnets pide un dato menos y muestra el
cupo que respalda la credencial.

**Problema detectado:**
El formulario preguntaba dos veces lo mismo: primero la actividad —pescador o
comercializador— y después el tipo de carnet, cuando cada tipo ya corresponde a
una sola actividad. Además, al emitir un carnet de pescador no se veía de qué
cupo de pesca iba a colgar, aunque el volumen de ese cupo es justamente lo que
se imprime en el plástico.

**Solución aplicada:**
Ahora se elige solo el tipo de carnet, y la lista está separada en dos grupos
—Pescador y Comercializador— para que se vea qué habilita cada uno. Al elegir un
tipo de pescador, la pantalla muestra el cupo de la persona: la escala, los
kilos disponibles, su estado y hasta cuándo vence. Si no tiene cupo, lo avisa
antes de intentar guardar; y si tuviera más de uno, aparece una lista para
elegir de cuál cuelga el carnet.

También se corrigió un aviso equivocado: a una persona con el cupo recién
solicitado —todavía sin cobrar— el sistema le decía que no tenía cupo, cuando en
realidad el carnet se puede emitir igual y los dos se pagan juntos.

**Detalle de archivos modificados:**
1. Pantalla de emisión de carnet: un campo menos y la tarjeta del cupo.
2. Buscador de personas: ahora informa también el cupo vigente.
3. Validaciones de emisión: la actividad se toma del tipo elegido.

**Resultado:** Se emite con menos pasos y viendo exactamente qué cupo queda
impreso en el carnet.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El tipo de embarcación pasa a ser obligatorio al otorgar un cupo.

**Problema detectado:**
El dato era opcional, así que se podía registrar un cupo sin declarar con qué
embarcación se sale a pescar. Cuando eso pasaba, la Autorización de Pesca que
imprime el sistema salía con ese renglón vacío y había que completarlo a mano,
que es justamente lo que el sistema viene a evitar.

**Solución aplicada:**
Ahora el campo es obligatorio: el formulario lo señala, el sistema no deja
guardar sin él y la base de datos tampoco lo acepta. Se sigue escribiendo
libremente —canoa, peque-peque, bote— porque no existe una lista oficial de
embarcaciones.

**Detalle de archivos modificados:**
1. Base de datos: la columna deja de admitir vacíos.
2. Registro y corrección del cupo: exigen el dato.
3. Pantallas de alta, corrección y ficha del cupo.

**Resultado:** La autorización impresa sale siempre con la embarcación
declarada.

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`).

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Se completaron las reglas de los módulos de Carnets, Permisos de
Faena y Guías de Transporte.

**Problema detectado:**
Tres cosas que el sistema permitía y no debía. Primero, el tipo de carnet y la
actividad no estaban atados: se podía emitir un carnet marcado como «pescador»
eligiendo el tipo «Carnet Comercializador», y el documento salía diciendo una
cosa mientras el sistema registraba otra. Segundo, la guía de transporte
guardaba por separado a la persona y al carnet, y nada obligaba a que fueran la
misma. Tercero, un carnet de comercializador podía terminar con un cupo de pesca
asociado, cuando el comercializador no pesca.

**Solución aplicada:**
Cada tipo de carnet del catálogo ahora indica para qué actividad sirve, y al
emitir solo se ofrecen los que corresponden; si no coinciden, el sistema lo
rechaza. La guía de transporte pasa a depender únicamente del carnet del
comercializador, de donde se toma la persona. Y un carnet de comercializador ya
no acepta cupo de pesca.

También se corrigió un mensaje de error que decía lo contrario de lo correcto:
al pescador le avisaba que su carnet sirve para emitir guías.

**Detalle de archivos modificados:**
1. Catálogo de tipos de carnet: se agregó la actividad, y se muestra en la
   pantalla.
2. Emisión de carnets: valida la coherencia entre el tipo y la actividad.
3. Guías de transporte: dependen del carnet del comercializador.
4. Permisos de faena y guías: se validan los plazos máximos (30 y 5 días).

**Resultado:** El documento que se entrega y lo que el sistema guarda dicen
siempre lo mismo.

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`).

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Los permisos de faena ahora dependen únicamente del carnet.

**Problema detectado:**
Cada permiso de faena guardaba dos referencias: el carnet del pescador y, por
separado, el cupo del que descontaba los kilos. Como eran independientes, un
permiso podía terminar descontando de un cupo que no era el del carnet con el
que se emitió, y el sistema no lo impedía.

**Solución aplicada:**
El permiso de faena queda ligado solo al carnet. El cupo se conoce a través de
él, porque el carnet ya guarda a qué aprovechamiento pertenece. El descuento de
kilos y el control de saldo funcionan igual que antes, pero ahora no hay forma
de que un permiso apunte al cupo equivocado.

La numeración de las hojas del talonario pasa a contarse por carnet: cada carnet
arranca en 1.

**Detalle de archivos modificados:**
1. Base de datos: se quitó la columna del cupo en los permisos de faena.
2. Reglas de emisión y cierre de faenas: toman el cupo desde el carnet.
3. Pantallas de faenas y de beneficiario: el número siguiente del talonario sale
   del carnet.

**Resultado:** No puede haber un permiso de faena descontando del cupo
equivocado.

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`).

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`), porque el estado cambió de nombre también
> por dentro.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Registrar los depósitos ahora pide una confirmación.

**Problema detectado:**
El botón que registra los depósitos también presenta el trámite a revisión: en
ese momento se emite el recibo que la persona se lleva y el trámite ya no se
puede corregir ni eliminar. Todo eso pasaba con un solo clic, así que un monto
mal escrito se descubría con el papel ya impreso.

**Solución aplicada:**
Antes de guardar se abre una ventana que dice cuántas boletas se van a cargar y
por qué monto total, para compararlo con los papeles que están sobre el
mostrador, y avisa que el trámite pasa a revisión y deja de poder modificarse.
Hay que marcar una casilla declarando que los montos y los números coinciden con
los comprobantes del banco.

**Detalle de archivos modificados:**
1. Pantalla del cupo: el botón de registrar abre la ventana de confirmación.

**Resultado:** Los depósitos se revisan una última vez contra el papel antes de
que se emita el recibo.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Las listas muestran primero lo que se registró recién.

**Problema detectado:**
Las listas de carnets y de autorizaciones de pesca se ordenaban por la fecha de
solicitud, que es la que escribe el funcionario y puede ser de días atrás cuando
se está poniendo al día un trámite hecho en papel. Un registro cargado hace un
minuto podía aparecer varias páginas más abajo, y había que buscarlo.

**Solución aplicada:**
Las dos listas se ordenan ahora por el momento en que se cargó el registro, que
es el mismo dato que muestra la columna «Registrado». Lo último cargado queda
siempre arriba.

**Detalle de archivos modificados:**
1. Lista de autorizaciones de pesca: nuevo orden.
2. Lista de carnets: nuevo orden.

**Resultado:** Lo que se acaba de registrar está siempre en la primera fila.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** La lista de carnets tiene la misma barra de búsqueda que las demás.

**Problema detectado:**
En la lista de carnets los controles de arriba estaban en otro orden que en el
padrón de beneficiarios y en la lista de autorizaciones de pesca, y había un
botón con una lupa que en las otras pantallas no existe.

**Solución aplicada:**
Se ordenó igual que en las demás: a la izquierda «Mostrar N registros» y a la
derecha, juntos, el filtro de actividad, el de estado y el buscador, con la
lupa dentro del casillero.

**Detalle de archivos modificados:**
1. Lista de carnets: la barra de arriba de la tabla.

**Resultado:** Las tres listas del sistema se usan de la misma manera.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Todas las listas del sistema se manejan igual.

**Problema detectado:**
Cada lista tenía los controles de búsqueda en un orden distinto, y en la de
asociaciones el buscador estaba en otro lugar y la lista mostraba todos los
registros de una sola vez, sin páginas.

**Solución aplicada:**
Las listas de permisos de faena, guías de movimiento, caja, recibos y
asociaciones quedaron con la misma barra que el padrón de beneficiarios: a la
izquierda «Mostrar N registros» y a la derecha los filtros junto al buscador,
con la lupa dentro del casillero. La lista de asociaciones ahora se muestra por
páginas y se puede filtrar por estado.

**Detalle de archivos modificados:**
1. Listas de faenas, guías, caja y recibos: la barra de arriba de la tabla.
2. Lista de asociaciones: la barra, el filtro de estado y las páginas.

**Resultado:** Quien aprende a usar una lista sabe usar todas.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** Los catálogos de escala y de tipos de carnet, al día.

**Problema detectado:**
Las listas de la escala de aprovechamiento y de los tipos de carnet no tenían
buscador ni páginas, y mostraban todo de una sola vez. Además, en tipos de
carnet se podían agregar nuevos desde el sistema, cuando esa lista viene de la
resolución y no se cambia por pantalla.

**Solución aplicada:**
Las dos listas quedaron con la misma barra que el resto del sistema: «Mostrar N
registros», un filtro —modalidad en la escala, actividad en los tipos— y el
buscador con la lupa dentro del casillero, y se muestran por páginas. En tipos
de carnet se quitó el botón de nuevo tipo y también se cerró por dentro, para
que no se pueda registrar por ningún camino; los existentes se siguen
corrigiendo.

**Detalle de archivos modificados:**
1. Escala de aprovechamiento: barra, filtro y páginas.
2. Tipos de carnet: lo mismo, y se retiró el alta.

**Resultado:** Los catálogos se consultan como las demás listas, y la lista de
tipos de carnet solo cambia donde debe cambiar.

---

**Informe de trabajo — 20/09/2026**
**Responsable:** nachoDev

**Actividad:** El permiso de faena ahora se paga y se aprueba como los demás trámites.

**Problema detectado:**
El permiso de salida a pescar se registraba y quedaba habilitado en el mismo
momento, sin pago y sin que nadie lo revisara. El carnet y la autorización de
aprovechamiento ya funcionaban de otra manera: se registran, se cargan los
depósitos, se presentan para revisión y recién cuando alguien los aprueba valen.

**Solución aplicada:**
La faena sigue ahora ese mismo camino. Al registrarla queda PENDIENTE y no
autoriza nada; se cargan los depósitos por el arancel de la salida, se envía a
revisión —y en ese momento se emite el recibo con el total—, y cuando la
aprueban queda habilitada con su fecha. Si algo no cuadra se rechaza con el
motivo escrito y vuelve a ventanilla sin perder los depósitos. También se puede
cobrar desde Caja, como los otros trámites.

Los kilos de la salida quedan apartados del cupo desde que se pide el permiso,
para que dos solicitudes no se lleven el mismo volumen.

**Detalle de archivos modificados:**
1. Permiso de faena: estados nuevos, arancel y fechas de solicitud y aprobación.
2. Reglas de cobro y aprobación de la faena.
3. Pantallas de faena: registro, lista y ficha con la tarjeta de pagos.
4. Caja y recibos: la faena entra como trámite cobrable.

> **Importante para el responsable:** hay que volver a generar la base
> (`php artisan migrate:fresh --seed`), y confirmar cuánto cuesta el permiso de
> faena: quedó cargado en 30 Bs a modo de ejemplo.

**Resultado:** No se puede salir a pescar con un permiso que nadie cobró ni
firmó.

---

*Documento de sesión — nachoDev — Gobierno Autónomo Departamental del Beni.*
